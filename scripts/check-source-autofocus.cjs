const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const {pathToFileURL}=require('node:url'),path=require('node:path'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage();await p.route(/^https?:/,r=>r.abort());await p.goto(pathToFileURL(path.resolve('ASMach_Teknik_Resim_Balonlama.html')).href);
 await p.evaluate(()=>{const h=document.createElement('div');h.id='focusTest';h.style.cssText='position:fixed;left:10px;top:10px;width:440px;z-index:9999;background:white';document.body.append(h);const c=document.createElement('canvas');c.width=3000;c.height=2000;const g=c.getContext('2d');g.fillStyle='white';g.fillRect(0,0,c.width,c.height);g.fillStyle='black';g.fillText('9',1500,1000);ASMachCandidateSource.mount(h,{candidate:{pageImage:c,box:{x:.5,y:.5,w:.004,h:.01}},candidates:[],isCurrent:()=>true});});
 for(const width of [440,360,460]){await p.locator('#focusTest').evaluate((h,w)=>h.style.width=w+'px',width);await p.evaluate(()=>new Promise(r=>requestAnimationFrame(()=>requestAnimationFrame(r))));const m=await p.locator('#focusTest').evaluate(h=>{const v=h.querySelector('.wf-source-viewport'),c=h.querySelector('canvas'),a=v.getBoundingClientRect(),b=c.getBoundingClientRect();return{digitWidth:b.width*.004,digitHeight:b.height*.01,dx:Math.abs(b.left+b.width*.502-(a.left+v.clientWidth/2)),dy:Math.abs(b.top+b.height*.505-(a.top+v.clientHeight/2)),viewportWidth:a.width,hostWidth:h.clientWidth,zoom:parseInt(h.querySelector('output').value)};});assert.ok(m.zoom<=1600,JSON.stringify(m));assert.ok(m.digitWidth<80&&m.digitHeight<110,JSON.stringify(m));assert.ok(m.dx<4&&m.dy<4,JSON.stringify(m));assert.ok(m.viewportWidth<=m.hostWidth,JSON.stringify(m));}
 console.log('PASS: single-digit autofocus remains moderate, centered after panel resize, no intrinsic width expansion.');
}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
