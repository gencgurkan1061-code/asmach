const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage();await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=800;c.height=600;await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'test.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[]});ASMachProduction.open('project');});
for(const tab of ['project','components','records','operations','controls','evidence']){await p.locator(`#productionWorkspace [data-tab="${tab}"]`).click();if(tab!=='project')await p.locator('#productionWorkspace [data-add]').click();const host=p.locator('#productionWorkspace [data-editor]');const missing=await host.locator('input,textarea,select').evaluateAll(es=>es.filter(e=>!e.disabled&&!e.readOnly&&['text','number','textarea','date','select-one'].includes(e.type)&&!e.closest('.po-input')).length);assert.equal(missing,0,tab);if(tab!=='project')await p.locator('.pr-record-modal [data-cancel]').click();}
await p.locator('#productionWorkspace [data-tab="components"]').click();await p.locator('#productionWorkspace [data-add]').click();await p.locator('[data-field="name"]').locator('..').locator('button').click();await p.locator('#projectOcrDialog [data-text]').fill('OCR parça');await p.locator('#projectOcrDialog [data-apply]').click();assert.equal(await p.locator('[data-field="name"]').inputValue(),'OCR parça');assert.equal(await p.locator('.pr-record-modal').count(),1);assert.equal(await p.evaluate(()=>ASMachProduction.data(ASMachApp.state).components.length),0);console.log('PASS OCR on all editable project/table forms; nested dialog transfer retains unsaved draft');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
