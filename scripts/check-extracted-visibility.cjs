const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage();const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=1000;c.height=800;await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'t.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[{id:'a',number:'1',page:1,type:'Çap',selectionBox:{x:.2,y:.2,w:.1,h:.05}}]});document.getElementById('rbTab-drawing').click();});
const before=await p.evaluate(()=>JSON.stringify(ASMachApp.state.annotations));await p.locator('#extractedVisibilityButton').click();assert.equal(await p.locator('[data-extracted-mask]').count(),1);assert.equal(await p.locator('.balloon-group[data-id=a]').count(),1);assert.equal(await p.evaluate(()=>JSON.stringify(ASMachApp.state.annotations)),before);await p.locator('#extractedVisibilityButton').click();assert.equal(await p.locator('[data-extracted-mask]').count(),0);assert.deepEqual(errors,[]);console.log('PASS toggle, source region mask, balloons retained, records unchanged, no page errors');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
