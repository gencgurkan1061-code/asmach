const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage(),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=800;c.height=600;await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'t.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[]});ASMachProduction.open('project');});await p.locator('[data-field=partNo]').fill('PN-123');assert.equal(await p.evaluate(()=>ASMachApp.state.metadata.partNo),'PN-123');await p.locator('[data-field="ir.lotSize"]').fill('-2');assert.notEqual(await p.evaluate(()=>ASMachApp.state.metadata.inspectionReport.lotSize),'-2');await p.locator('[data-field="ir.lotSize"]').fill('20');await p.locator('#productionWorkspace [data-tab=operations]').click();await p.locator('#productionWorkspace [data-tab=project]').click();assert.equal(await p.locator('[data-field=partNo]').inputValue(),'PN-123');assert.equal(await p.locator('#productionWorkspace [data-save]').count(),0);assert.equal(await p.locator('[data-project-save]').textContent(),'Proje bilgilerini kaydet');assert.deepEqual(errors,[]);console.log('PASS automatic input persistence, validation, tab navigation and removed form save');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1});
