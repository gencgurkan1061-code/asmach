const fs=require('node:fs');let fixture=fs.readFileSync('scripts/check-characteristic-info.cjs','utf8').split('\n').slice(0,4).join('\n');
fixture+=`\nconst result=await p.evaluate(()=>{const app=ASMachApp,r=app.selectedAnnotation();ASMachRoleAppearance.setMode(app.state,r,'fixed','');r.boxLineStyle='none';r.arrowEnabled=false;r.bubbleX=.4;r.bubbleY=.32;app.renderAll();const selected=document.querySelectorAll('.selection-box-guide').length,position=[r.bubbleX,r.bubbleY];app.state.selectedId=null;ASMachCharacteristicUI.selectIds([]);app.renderAll();const deselected=document.querySelectorAll('.selection-box-guide').length;app.state.selectedId=r.id;app.renderAll();return{selected,deselected,position,stable:r.bubbleX===position[0]&&r.bubbleY===position[1]};});assert.equal(result.selected,1);assert.equal(result.deselected,0);assert.ok(result.stable);assert.deepEqual(errors,[]);await p.screenshot({path:'outputs/hidden-box-selection.png'});console.log('PASS hidden box selection guide, deselection, protected measure area and stable render');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});`;eval(fixture);
