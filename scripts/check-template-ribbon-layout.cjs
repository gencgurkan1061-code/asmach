const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),assert=require('node:assert/strict'),{pathToFileURL}=require('node:url'),path=require('node:path');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1500,height:850}});await p.route(/^https?:/,r=>r.abort());await p.goto(pathToFileURL(path.resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(()=>ASMachRibbon.select('format'));const info=await p.evaluate(()=>{const b=document.getElementById('ribbonTemplateEditor'),n=document.getElementById('viewNumberingButton'),s=getComputedStyle(b),r=b.getBoundingClientRect(),nr=n.getBoundingClientRect();return{after:n.nextElementSibling===b,sameParent:b.parentElement===n.parentElement,icon:b.querySelector('img').src===ASMachEditIcons.template,background:s.backgroundColor,border:s.borderTopColor,height:r.height,otherHeight:nr.height,y:r.y,otherY:nr.y};});assert.ok(info.after&&info.sameParent&&info.icon);assert.equal(info.background,'rgba(0, 0, 0, 0)');assert.equal(info.border,'rgba(0, 0, 0, 0)');assert.equal(info.height,info.otherHeight);assert.equal(info.y,info.otherY);await p.screenshot({path:'outputs/template-ribbon-layout.png'});console.log('PASS adjacent placement, provided SVG, transparent styling and equal alignment');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
