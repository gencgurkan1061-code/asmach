const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const {pathToFileURL}=require('node:url'),path=require('node:path'),assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true});try{
 const p=await browser.newPage({viewport:{width:1600,height:950}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.route(/^https?:/,r=>r.abort());await p.goto(pathToFileURL(path.resolve('ASMach_Teknik_Resim_Balonlama.html')).href);
 await p.evaluate(async()=>{const canvas=document.createElement('canvas');canvas.width=1000;canvas.height=700;const g=canvas.getContext('2d');g.fillStyle='white';g.fillRect(0,0,1000,700);g.fillStyle='black';g.font='24px Arial';g.fillText('20 ±0.1',100,100);const source=canvas.toDataURL();await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'lifecycle.png',type:'image/png',dataUrl:source},annotations:[],pendingCandidates:[0,1,2].map(i=>({selected:i===0,page:1,box:{x:.1,y:.1+i*.18,w:.18,h:.05},text:(20+i)+' ±0.1',parsed:ASMachApp.parseRequirement((20+i)+' ±0.1'),snapshot:source,confidence:95,plan:{inspectionMethod:ASMachApp.getMethods()[0].value,inspectionFrequency:'EACH_PART'}}))});window.ASMachAutomaticGeometry={...ASMachAutomaticGeometry,place:()=>true};});
 await p.locator('#autoDetectButton').click();await p.locator('#wfAccept').click();assert.equal(await p.evaluate(()=>ASMachApp.state.annotations.length),1);assert.equal(await p.evaluate(()=>ASMachWorkflow.exportQueue().length),2);assert.equal(await p.locator('#wfRows tr[data-index]').count(),2);
 await p.locator('#wfShowAdded').click();assert.equal(await p.locator('#wfRows tr[data-index]').count(),3);const added=p.locator('#wfRows tr').filter({has:p.locator('td[data-column="state"]', {hasText:'Eklendi'})});assert.equal(await added.count(),1);assert.equal(await added.locator('input[type=checkbox]').isDisabled(),true);await added.locator('td[data-column="requirement"]').click();assert.equal(await p.locator('.wf-candidate-editor input[aria-label="Algılanan metin"]').isDisabled(),true);assert.equal(await p.locator('.wf-inline-source button').filter({hasText:'Yeniden seç'}).isVisible(),false);
 await p.locator('#wfSelectAll').check();assert.equal(await p.evaluate(()=>ASMachWorkflow.exportQueue().filter(c=>c.selected).length),2);await p.locator('#wfSelectAll').uncheck();assert.equal(await p.locator('#wfAccept').isDisabled(),true);
 p.once('dialog',d=>d.dismiss());await p.getByRole('button',{name:'Aday 1 sil',exact:true}).click();assert.equal(await p.evaluate(()=>ASMachWorkflow.exportQueue().length),2);
 p.once('dialog',d=>d.accept());await p.getByRole('button',{name:'Aday 1 sil',exact:true}).click();assert.equal(await p.evaluate(()=>ASMachWorkflow.exportQueue().length),1);assert.equal(await p.evaluate(()=>ASMachApp.state.annotations.length),1);await p.locator('#wfUndoCandidateDelete').click();assert.equal(await p.evaluate(()=>ASMachWorkflow.exportQueue().length),2);
 await p.locator('#wfSelectAll').check();p.once('dialog',d=>d.accept());await p.locator('#wfDeleteCandidates').click();assert.equal(await p.evaluate(()=>ASMachWorkflow.exportQueue().length),0);assert.equal(await p.evaluate(()=>ASMachApp.state.annotations.length),1);assert.equal(await p.locator('#wfRows tr[data-index]').count(),1);assert.equal(await p.locator('#wfAccept').isDisabled(),true);
 await p.locator('#wfUndoCandidateDelete').click();assert.equal(await p.evaluate(()=>ASMachWorkflow.exportQueue().length),2);await p.locator('#wfSelectAll').uncheck();
 await p.locator('#wfClose').click();await p.evaluate(async()=>{window.savedProject=JSON.parse(JSON.stringify(ASMachApp.projectPayload()));ASMachApp.state.fileData=null;await ASMachApp.restoreProject(savedProject);});await p.locator('#autoDetectButton').click();assert.equal(await p.locator('#wfRows tr[data-index]').count(),3);assert.equal(await p.evaluate(()=>ASMachApp.state.annotations[0].autoDetected),true);
 await p.locator('#wfShowAdded').click();assert.equal(await p.locator('#wfRows tr[data-index]').count(),2);await p.locator('#wfClose').click();await p.locator('#autoDetectButton').click();assert.equal(await p.locator('#wfRows tr[data-index]').count(),2);await p.locator('#wfShowAdded').click();
 await p.locator('#wfClose').click();await p.evaluate(()=>{delete ASMachApp.state.annotations[0].autoDetected;});await p.locator('#autoDetectButton').click();assert.equal(await p.locator('#wfRows tr[data-index]').count(),3,'Legacy audit identifies previously added records');
 await p.screenshot({path:'outputs/safety-review-qa/candidate-lifecycle.png'});await p.locator('#wfClose').click();await p.evaluate(()=>ASMachApp.state.annotations=[]);await p.locator('#autoDetectButton').click();assert.equal(await p.locator('#wfRows tr[data-index]').count(),2,'No ghost saved row after external deletion');assert.deepEqual(errors,[]);
 console.log('PASS: saved show/hide, read-only, no duplicate add, single/bulk delete, cancel, undo, project persistence, legacy added records and external deletion.');
}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
