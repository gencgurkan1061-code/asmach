const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true});try{
 const p=await browser.newPage({viewport:{width:1440,height:900}}),errors=[];p.on('pageerror',e=>errors.push(e.message));
 await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);
 await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=1000;c.height=800;await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'test.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[]});ASMachApp.state.annotations=[1,2,3].map(n=>ASMachApp.normalizeAnnotation({id:'r'+n,number:String(n),type:'Uzunluk',nominalValue:'20',unit:'mm',requirement:'20',page:1,size:n===2?40:36}));ASMachApp.renderAll();ASMachCharacteristicUI.selectIds(['r1','r2']);});
 await p.locator('#rbTab-format').click();await p.waitForTimeout(450);
 assert.equal(await p.locator('#rbPanel-format [data-count]').textContent(),'2 seçili');
 assert.equal(await p.locator('[data-style-field=size]').getAttribute('placeholder'),'Karışık');
 assert.ok((await p.locator('#rbPanel-format').boundingBox()).height<130);
 await p.getByText('Balon ▾',{exact:true}).click();
 await p.locator('[data-style-field=size]').fill('52');await p.locator('[data-style-field=size]').press('Tab');
 assert.deepEqual(await p.evaluate(()=>ASMachApp.state.annotations.map(r=>r.size)),[52,52,36]);
 await p.evaluate(()=>document.getElementById('undoButton').click());assert.deepEqual(await p.evaluate(()=>ASMachApp.state.annotations.map(r=>r.size)),[36,40,36]);
 await p.evaluate(()=>document.getElementById('redoButton').click());assert.deepEqual(await p.evaluate(()=>ASMachApp.state.annotations.map(r=>r.size)),[52,52,36]);
 await p.evaluate(()=>ASMachCharacteristicUI.selectIds(['r3']));await p.waitForTimeout(400);assert.equal(await p.locator('#rbPanel-format [data-count]').textContent(),'1 seçili');
 await p.locator('[data-mode=bulk]').click();assert.equal(await p.locator('#acuiBulkEditor').evaluate(e=>e.matches(':modal')),false);
 await p.getByText('Karakteristik bilgileri ▾',{exact:true}).click();
 await p.locator('[data-enable=zone]').check();await p.locator('[data-bulk=zone]').fill('B3');await p.locator('[data-preview-button]').click();assert.equal(await p.locator('#frBulkPreview').evaluate(e=>e.matches(':modal')),true);await p.locator('#frBulkPreview footer [data-cancel]').click();assert.deepEqual(await p.evaluate(()=>ASMachApp.state.annotations.map(r=>r.zone||'')),['','','']);await p.locator('[data-preview-button]').click();await p.locator('#frBulkPreview [data-confirm]').click();
 assert.deepEqual(await p.evaluate(()=>ASMachApp.state.annotations.map(r=>r.zone||'')),['','','B3']);
 await p.screenshot({path:'outputs/format-ribbon-bulk.png'});
 await p.locator('[data-mode=style]').click();await p.setViewportSize({width:1366,height:768});await p.screenshot({path:'outputs/format-ribbon.png'});
 await p.getByText('Balon konumu ▾',{exact:true}).click();await p.locator('[data-direction=right]').click();assert.equal(await p.locator('[data-style-field=boxDirection]').inputValue(),'right');assert.equal(await p.locator('[data-apply-style]').count(),0);
 await p.screenshot({path:'outputs/format-position-menu.png'});await p.keyboard.press('Escape');assert.equal(await p.locator('.fr-dropdown[open]').count(),0);
 await p.getByText('Balon ▾',{exact:true}).click();
 await p.locator('[data-style-field=size]').fill('999');await p.locator('[data-style-field=size]').press('Tab');assert.equal(await p.evaluate(()=>ASMachApp.state.annotations[2].size),36);
 await p.evaluate(()=>{document.querySelectorAll('dialog.app-message[open]').forEach(d=>d.close());Object.assign(ASMachApp.state.annotations[1],{type:'Diş',threadClass:'6H'});ASMachCharacteristicUI.selectIds(['r1','r2']);});await p.waitForTimeout(400);await p.locator('[data-mode=bulk]').click();await p.getByText('Karakteristik bilgileri ▾',{exact:true}).click();await p.locator('[data-enable=type]').check();await p.locator('[data-bulk=type]').selectOption('Çap');const invalidBefore=await p.evaluate(()=>JSON.stringify(ASMachApp.state.annotations[1]));await p.locator('[data-preview-button]').click();assert.equal(await p.locator('#frBulkPreview [data-confirm]').isEnabled(),true);
 const box=await p.locator('#frBulkPreview').boundingBox();await p.mouse.move(box.x+60,box.y+18);await p.mouse.down();await p.mouse.move(box.x+110,box.y+48,{steps:4});await p.mouse.up();assert.ok((await p.locator('#frBulkPreview').boundingBox()).x>box.x+40);await p.locator('#frBulkPreview [data-confirm]').click();assert.equal(await p.evaluate(()=>ASMachApp.state.annotations[0].type),'Çap');assert.equal(await p.evaluate(()=>JSON.stringify(ASMachApp.state.annotations[1])),invalidBefore);
 assert.deepEqual(errors,[]);console.log('PASS formatting, draggable preview, partial valid application and unchanged invalid record');
}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
