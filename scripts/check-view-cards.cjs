const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1600,height:950}});await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=1000;c.height=700;const g=c.getContext('2d');g.fillStyle='white';g.fillRect(0,0,1000,700);g.strokeRect(100,100,200,200);g.font='40px Arial';g.fillStyle='black';g.fillText('Ø25',120,210);await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'test.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[]});for(const [x,name]of [[.1,'Görünüş 1'],[.5,'Görünüş 2']])ASMachViewNumbering.registerScanArea(ASMachApp,{page:1,name,box:{x,y:.1,w:.3,h:.4}});});await p.locator('#autoDetectButton').evaluate(e=>e.click());assert.equal(await p.locator('.wf-advanced').count(),0);assert.equal(await p.locator('#wfStop').isVisible(),false);assert.ok(await p.locator('#wfDetailed').isVisible());await p.locator('#wfDetailed').check();assert.ok(await p.locator('#wfDetailed').isChecked());assert.equal(await p.locator('#wfRun svg path').count(),6);const card=p.locator('.wf-area-card[data-area]').first();assert.equal(await p.locator('.wf-area-card[data-area]').count(),2);assert.ok(await card.locator('input[type=checkbox]').isChecked());await card.locator('span').click();assert.ok(await card.locator('input[type=checkbox]').isChecked());await card.locator('.wf-area-preview').click();await p.waitForFunction(()=>document.querySelector('#wfAreaPreview footer')?.textContent.includes('Tarama seçiminiz değişmedi'));assert.ok(await card.locator('input[type=checkbox]').isChecked());assert.deepEqual(await p.locator('#wfAreaPreview canvas').evaluate(c=>[c.width,c.height]),[1000,700]);assert.ok(await p.locator('#wfAreaPreview canvas').evaluate(c=>c.getContext('2d').getImageData(101,100,1,1).data[1]>0));await p.screenshot({path:'outputs/full-page-view-preview.png'});const old=await p.locator('#wfAreaPreview output').textContent();await p.keyboard.press('Control+-');assert.notEqual(await p.locator('#wfAreaPreview output').textContent(),old);await p.keyboard.press('Escape');await card.locator('input[type=checkbox]').uncheck();assert.equal(await p.locator('#wfRun').getAttribute('aria-label'),'Seçilen görünüşleri tara (1)');const label=await card.locator('.wf-area-delete').getAttribute('aria-label');assert.match(label,/Görünüş 1/);await p.evaluate(()=>{ASMachMessages.confirm=async message=>{window.lastDeleteMessage=message;return false;};});await card.locator('.wf-area-delete').click();assert.match(await p.evaluate(()=>window.lastDeleteMessage),/Görünüş 1/);assert.equal(await p.locator('.wf-area-card[data-area]').count(),2);await p.screenshot({path:'outputs/compact-view-cards.png'});console.log('PASS checkbox-only selection, preview and zoom without selection changes, named per-card delete/cancel');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
