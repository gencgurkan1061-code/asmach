const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const {pathToFileURL}=require('node:url'),path=require('node:path'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1366,height:900}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.route(/^https?:/,r=>r.abort());await p.goto(pathToFileURL(path.resolve('ASMach_Teknik_Resim_Balonlama.html')).href);
 await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=800;c.height=600;const g=c.getContext('2d');g.fillStyle='white';g.fillRect(0,0,800,600);g.strokeRect(200,240,350,200);g.fillStyle='#0751c9';g.font='25px Arial';g.fillText('2.5 ±0.2 × 45°',220,200);await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'source.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[{id:'p',number:'7',page:1,...ASMachApp.parseRequirement('2.5 ±0.2 × 45°'),type:'Pah',chamferAngle:'45',nominalValue:'2.5',lowerTolerance:'-0.2',upperTolerance:'+0.2',lowerLimit:'2.3',upperLimit:'2.7',unit:'mm',ocrText:'2.5 ±0.2 × 45°',requirement:'Özel pah açıklaması',requirementMode:'manual',manualFields:['requirement'],selectionBox:{x:.27,y:.28,w:.27,h:.07},anchorX:.4,anchorY:.32,bubbleX:.58,bubbleY:.25}]});ASMachApp.state.selectedId='p';ASMachApp.renderAll();});
 assert.equal(await p.locator('#selectionForm [role=tab]').count(),3);assert.equal(await p.locator('#cwSourceToggle').getAttribute('aria-expanded'),'false');await p.locator('.ic-advanced>summary').click();
 const positions=await p.locator('#nominalValue,#lowerTolerance,#cwExtra_chamferAngle,[data-limit=lowerLimit],#ipGeneralChoice,#requirement').evaluateAll(ns=>ns.map(n=>({id:n.id||n.dataset.limit,x:n.getBoundingClientRect().x})));assert.ok(Math.max(...positions.map(n=>n.x))-Math.min(...positions.map(n=>n.x))<2,JSON.stringify(positions));
 await p.screenshot({path:'outputs/characteristic-workbench-qa/aligned-fields.png'});
 const before=await p.evaluate(()=>JSON.stringify(ASMachApp.state.annotations));await p.locator('#cwSourceToggle').click();await p.locator('#ipSourcePane canvas').first().waitFor({state:'visible'});const viewport=p.locator('#ipSourcePane .wf-source-viewport');const small=await viewport.boundingBox();await p.locator('#cwSourceGrow').click();const large=await viewport.boundingBox();assert.ok(large.height>small.height+100);await p.screenshot({path:'outputs/characteristic-workbench-qa/inline-source-expanded.png'});
 await p.locator('#ipSourcePane').getByRole('button',{name:'Kaynak görüntüyü yakınlaştır',exact:true}).click();assert.equal(await p.evaluate(()=>JSON.stringify(ASMachApp.state.annotations)),before);
 await p.locator('#ipSourcePane').getByRole('button',{name:'Yeniden seç',exact:true}).click();assert.equal(await p.evaluate(()=>ASMachApp.state.inlineReselectionId),'p');await p.locator('#cwSourceToggle').click();assert.equal(await p.evaluate(()=>ASMachApp.state.inlineReselectionId),null);assert.equal(await p.evaluate(()=>JSON.stringify(ASMachApp.state.annotations)),before);
 await p.locator('#cwSourceToggle').click();assert.equal(await p.locator('.cw-source-text>summary').count(),0);assert.ok(await p.locator('#balloonPage').isVisible());assert.ok(await p.locator('#balloonZone').isVisible());assert.equal(await p.locator('.cw-source-preview>.wf-source-tools').count(),2);await p.locator('#ciOcrText').fill('2.5 ±0.3 × 45°');await p.waitForFunction(()=>ASMachApp.selectedAnnotation().ocrText==='2.5 ±0.3 × 45°');assert.equal(await p.evaluate(()=>ASMachApp.state.annotations[0].requirement),'Özel pah açıklaması');assert.equal(await p.locator('#lowerTolerance').inputValue(),'-0.3');
 const sourceFields=await p.locator('#ciOcrText,#ciSymbolsButton,#balloonPage,#balloonZone,#toleranceStandard').evaluateAll(ns=>ns.map(n=>({id:n.id,x:n.getBoundingClientRect().x})));assert.ok(Math.max(...sourceFields.map(n=>n.x))-Math.min(...sourceFields.map(n=>n.x))<2,JSON.stringify(sourceFields));assert.equal(await p.locator('#selectionSnapshotImage').evaluate(n=>!!n.closest('.field').getClientRects().length),false);await p.locator('#toleranceStandard').scrollIntoViewIfNeeded();await p.screenshot({path:'outputs/characteristic-workbench-qa/source-fields-aligned.png'});
 await p.locator('#cwSourceToggle').click();await p.locator('#cwInspectorTab0').focus();await p.keyboard.press('ArrowRight');assert.equal(await p.locator('#cwInspectorTab2').getAttribute('aria-selected'),'true');await p.keyboard.press('ArrowLeft');assert.equal(await p.locator('#cwInspectorTab0').getAttribute('aria-selected'),'true');assert.deepEqual(errors,[]);console.log('PASS aligned measurement and source fields; inline source expand/hide, zoom, cancel, OCR bindings, manual text preservation and 3-tab keyboard navigation.');
 await p.locator('#cwQuantity').fill('3');await p.locator('#cwQuantity').press('Tab');assert.equal(await p.evaluate(()=>ASMachApp.state.annotations.filter(r=>r.listOnly).length),3);assert.equal(await p.locator('#overlay .balloon-group').count(),1);assert.equal(await p.locator('[popovertarget=cwCopies]').count(),0);const beforeQuantity=await p.evaluate(()=>JSON.stringify(ASMachApp.state.annotations));p.once('dialog',d=>d.dismiss());await p.locator('#cwQuantity').fill('1');await p.locator('#cwQuantity').press('Tab');assert.equal(await p.evaluate(()=>JSON.stringify(ASMachApp.state.annotations)),beforeQuantity);p.once('dialog',d=>d.accept());await p.locator('#cwQuantity').fill('1');await p.locator('#cwQuantity').press('Tab');assert.equal(await p.evaluate(()=>ASMachApp.state.annotations.length),1);const dock=await p.evaluate(()=>({footer:document.querySelector('#selectionForm .ip-footer').getBoundingClientRect().bottom,panel:document.querySelector('.inspector').getBoundingClientRect().bottom}));assert.ok(Math.abs(dock.footer-dock.panel)<5,JSON.stringify(dock));await p.screenshot({path:'outputs/characteristic-workbench-qa/quantity-docked.png'});console.log('PASS quantity expansion, cancellation, confirmed reduction and docked footer.');
}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
