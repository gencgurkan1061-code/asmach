const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1500,height:1000}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=1000;c.height=800;await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'t.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[{id:'a',number:'1',type:'GD&T',gdtSubtype:'position',upperTolerance:'0.1',requirement:'⌖ | 0.1 | A',requirementMode:'manual'}]});ASMachApp.state.selectedId='a';ASMachApp.renderAll();});const rich=p.locator('#requirement').locator('xpath=preceding-sibling::div[contains(@class,"gt-editor")]');await rich.locator('.gt-rich').waitFor({state:'visible'});await rich.locator('.gt-rich').fill('');await rich.getByText('Ω Sembol / GD&T',{exact:true}).click();await p.locator('.gt-palette [data-frame]').click();await rich.locator('.gt-cell').nth(1).click();await p.keyboard.type('0.05');await p.keyboard.press('Tab');await p.keyboard.type('A');assert.ok((await p.locator('#requirement').inputValue()).includes('0.05 | A'));await rich.getByText('Ω Sembol / GD&T',{exact:true}).click();assert.equal(await p.locator('.gt-palette select').count(),0);await p.locator('.gt-symbols button[aria-label="Ⓜ"]').click();assert.ok((await p.locator('#requirement').inputValue()).includes('Ⓜ'));const count=await rich.locator('.gt-cell').count();await rich.locator('.gt-cell').nth(1).click();await rich.locator('[data-cell-action=after]').click();assert.equal(await rich.locator('.gt-cell').count(),count+1);await p.keyboard.type('B');assert.ok((await p.locator('#requirement').inputValue()).includes('B'));await rich.locator('[data-cell-action=delete]').click();assert.equal(await rich.locator('.gt-cell').count(),count);await rich.getByText('Düz metin',{exact:true}).click();assert.equal(await p.locator('#requirement').isVisible(),true);assert.deepEqual(errors,[]);console.log('PASS editable GD&T frame, cell navigation, insertion at caret, backing text, plain-text fallback');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
