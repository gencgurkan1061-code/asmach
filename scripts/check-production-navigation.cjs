const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),{pathToFileURL}=require('node:url'),path=require('node:path'),assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true});try{const p=await browser.newPage({viewport:{width:1500,height:950}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.route(/^https?:/,r=>r.abort());await p.goto(pathToFileURL(path.resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(()=>{const a=ASMachApp;a.state.fileData='fixture';a.state.fileName='test.png';a.state.pageCount=1;a.elements.sourceCanvas.width=1000;a.elements.sourceCanvas.height=700;a.state.annotations=[a.normalizeAnnotation({id:'a1',number:'1',page:1,...a.parseRequirement('20 ±0.1')})];a.renderAll();});
 for(let i=0;i<3;i++){await p.locator('#rbTab-production').click();await p.locator('#rbTab-reports').click();await p.locator('[data-rw-report="control-plan"]').click();await p.locator('[data-rw-config]').click();await p.locator('.inspection-report-dialog').waitFor({state:'visible'});}
 assert.equal(await p.locator('.inspection-report-dialog').count(),1);assert.equal(await p.locator('#productionWorkspace').count(),1);assert.equal(await p.locator('#rbTab-reports').getAttribute('aria-selected'),'true');
 await p.locator('.inspection-report-dialog [data-option=workOrder]').fill('DRAFT-KEEP');await p.locator('#rbTab-production').click();assert.equal(await p.locator('#productionWorkspace nav #projectSetupButton').count(),0);assert.equal(await p.locator('#productionWorkspace [data-tab=project]').count(),1);
 await p.locator('#productionWorkspace [data-tab=operations]').click();await p.locator('#rbTab-reports').click();await p.locator('[data-rw-report="control-plan"]').click();await p.locator('[data-rw-config]').click();assert.equal(await p.locator('.inspection-report-dialog [data-option=workOrder]').inputValue(),'DRAFT-KEEP');await p.locator('#rbTab-production').click();assert.equal(await p.locator('#productionWorkspace [data-tab=operations]').getAttribute('aria-selected'),'true');
 await p.locator('#productionWorkspace [data-tab=project]').click();assert.equal(await p.locator('#productionWorkspace [data-reports]').count(),0);assert.equal(await p.locator('#productionWorkspace').getByText('Kontrol raporları',{exact:false}).count(),0);
 await p.evaluate(()=>{ASMachApp.state.metadata.production={controls:[{id:'control1',name:'Boyut kontrolü',code:'KP-1'}]};ASMachProduction.open('controls','control1');});await p.locator('.pr-record-modal').waitFor({state:'visible'});assert.equal(await p.locator('.pr-record-modal [data-field=name]').inputValue(),'Boyut kontrolü');await p.locator('.pr-record-modal [data-cancel]').click();
 await p.evaluate(()=>ASMachProduction.close(true));await p.locator('#rbTab-production').click();assert.equal(await p.locator('#productionWorkspace').count(),1);await p.screenshot({path:'outputs/safety-review-qa/production-navigation.png'});assert.deepEqual(errors,[]);console.log('PASS: single project entry, report reuse, draft preservation/discard, retained tabs, direct control editing, close/reopen without errors.');
}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
