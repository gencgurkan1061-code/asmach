const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),{pathToFileURL}=require('node:url'),path=require('node:path'),assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true});try{const p=await browser.newPage({viewport:{width:1600,height:950}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.route(/^https?:/,r=>r.abort());await p.goto(pathToFileURL(path.resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(()=>{const a=ASMachApp;a.state.fileData='fixture';a.state.fileName='test.png';a.state.pageCount=1;a.elements.sourceCanvas.width=1000;a.elements.sourceCanvas.height=700;a.renderAll();});
 assert.equal(await p.locator('.rb-tab').count(),6);await p.locator('#rbTab-drawing').click();
 assert.equal(await p.locator('#rbPanel-drawing .rb-group:visible').count(),5);assert.equal(await p.locator('.rb-caption:visible').count(),0);assert.ok(await p.locator('.rb-panels').evaluate(n=>n.getBoundingClientRect().height)<50);
 await p.locator('#boxOcrModeButton').click();assert.equal(await p.locator('.qt-options').first().isVisible(),true);assert.equal(await p.locator('#rbPanel-drawing .rb-group:visible').count(),7);
 await p.locator('#rbTab-view').click();await p.locator('#balloonSettingsButton').click();assert.equal(await p.locator('#balloonSettingsModal').isVisible(),true);assert.equal(await p.evaluate(()=>ASMachApp.state.mode),'select');assert.equal(await p.locator('.rb-panels').isVisible(),true);assert.equal(await p.locator('#balloonSettingsButton').isVisible(),true);
 await p.locator('#rbTab-drawing').click();assert.equal(await p.locator('#balloonSettingsModal').isVisible(),false);await p.locator('#manualToolsButton').click();assert.equal(await p.locator('#manualToolOptions').isVisible(),true);
 await p.locator('#rbTab-reports').click();assert.equal(await p.locator('#reportCenter').isVisible(),true);await p.locator('#rbTab-help').click();assert.equal(await p.locator('#keyboardHelp').isVisible(),true);
 await p.locator('#rbTab-file').click();assert.equal(await p.locator('#openFileButton').isVisible(),true);assert.equal(await p.locator('#rbPanel-file summary').count(),0);
 await p.locator('#rbTab-drawing').click();await p.locator('#boxOcrModeButton').click();await p.screenshot({path:'outputs/safety-review-qa/ribbon.png'});
 await p.setViewportSize({width:850,height:750});assert.equal(await p.locator('.toolbar').evaluate(n=>n.scrollWidth<=n.clientWidth+1),true);await p.locator('.rb-collapse').click();assert.equal(await p.locator('.rb-panels').isVisible(),false);await p.locator('.rb-collapse').click();assert.equal(await p.locator('.rb-panels').isVisible(),true);assert.deepEqual(errors,[]);console.log('PASS: ribbon tabs, OCR context, preserved screens, reports/help/file, collapse, responsive width.');
 }finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
