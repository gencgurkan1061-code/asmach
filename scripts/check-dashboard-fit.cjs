const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage();await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(()=>ASMachApp.openBalloonSettings());for(const [width,height] of [[1920,1080],[1366,768]]){await p.setViewportSize({width,height});await p.waitForTimeout(100);const metrics=await p.locator('.studio-inspector:visible').evaluate(e=>({h:e.clientHeight,sh:e.scrollHeight,w:e.clientWidth,sw:e.scrollWidth}));console.log(width,metrics);assert.ok(metrics.sh<=metrics.h+2,'Vertical overflow');assert.ok(metrics.sw<=metrics.w+2,'Horizontal overflow');await p.screenshot({path:'outputs/dashboard-fit-'+width+'.png'});}await p.locator('[data-style-area=classes]').click();const metrics=await p.locator('.studio-inspector:visible').evaluate(e=>({h:e.clientHeight,sh:e.scrollHeight}));assert.ok(metrics.sh<=metrics.h+2,JSON.stringify(metrics));console.log('PASS desktop dashboard fit, methods and classifications');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
