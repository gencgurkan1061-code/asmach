const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1500,height:1000}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);
await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=1000;c.height=800;await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'t.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[{id:'a',number:'2',quantity:'6',page:1,type:'Çap',nominalValue:'6',requirement:'6 × Ø6',selectionBox:{x:.4,y:.4,w:.1,h:.1}}]});ASMachApp.state.selectedId='a';ASMachApp.renderAll();});
assert.equal(await p.evaluate(()=>ASMachApp.state.annotations.length),6);await p.evaluate(()=>ASMachApp.renderAll());assert.equal(await p.evaluate(()=>ASMachApp.state.annotations.length),6);assert.equal(await p.locator('#cwQuantity').inputValue(),'6');assert.equal(await p.locator('tr[data-id=a] .repeat-balloon-preview svg').count(),1);
await p.locator('[data-repeat-toggle]').evaluate(e=>e.click());assert.equal(await p.locator('[data-repeat-parent]').evaluateAll(es=>es.every(e=>e.hidden)),true);await p.locator('[data-repeat-toggle]').evaluate(e=>e.click());
await p.evaluate(()=>ASMachProjectWorkspace.setup());await p.locator('#projectSetup [data-key=partNo]').fill('0012-A');assert.equal(await p.evaluate(()=>ASMachApp.state.metadata.partNo),'0012-A');assert.equal(await p.locator('#projectSetup .po-input button').count(),11);
await p.locator('#projectSetup [data-key=partNo]').locator('..').locator('button').click();assert.equal(await p.locator('#projectSetup #projectOcrDialog').count(),1);await p.locator('#projectOcrDialog [data-plus]').click();await p.locator('#projectOcrDialog [data-text]').fill('0009-B');await p.locator('#projectOcrDialog [data-apply]').click();assert.equal(await p.evaluate(()=>ASMachApp.state.metadata.partNo),'0009-B');assert.equal(await p.locator('#projectSetup').count(),1);
await p.locator('#projectSetup [data-key=standard]').selectOption('CUSTOM');assert.equal(await p.locator('#projectSetup [data-key=lower]').isVisible(),true);await p.locator('#projectSetup [data-key=standard]').selectOption('');assert.equal(await p.locator('#projectSetup [data-key=lower]').isVisible(),false);
await p.locator('#projectSetup [data-cancel]').click();assert.equal(await p.evaluate(()=>ASMachApp.state.metadata.partNo),'0009-B');assert.deepEqual(errors,[]);console.log('PASS automatic quantity totals/idempotence, group toggle, number SVG, preparation autosave, inline OCR transfer, leading zeros, dynamic tolerance and close persistence');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1});
