const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true});try{const page=await browser.newPage();await page.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);const result=await page.evaluate(async()=>{let received;window.ASMachDesktop={editExcelTemplate:async(id,fields)=>{received={id,fields};return false;}};await ASMachExcelCellTemplate.open(ASMachApp,'selected.xltx');const path='xl/worksheets/sheet1.xml',template={format:'imported',direction:'rows',bindings:{},parts:{'[Content_Types].xml':{text:'spreadsheetml.template.main+xml'},[path]:{text:'<worksheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main"><dimension ref="A1"/><sheetData><row r="1"><c r="A1" t="inlineStr"><is><t>{{record.number}}</t></is></c></row></sheetData></worksheet>'}},sourceSheets:[{path,name:'Test'}]};ASMachExcelTemplateImport.loadSheet(template,0);const field=template.cells['0:0'].field,output=ASMachExcelTemplateImport.files(template,{annotations:[{number:'27.2'},{number:'28.4'}],metadata:{}},false)[path];return{selected:received.id,hasField:received.fields.some(item=>item[0]==='record.number'),grid:!!document.getElementById('cellTemplateEditor'),field,output};});assert.equal(result.selected,'selected.xltx');assert.equal(result.hasField,true);assert.equal(result.grid,false);assert.equal(result.field,'record.number');assert.match(result.output,/27.2/);assert.match(result.output,/28.4/);assert.doesNotMatch(result.output,/\{\{record/);console.log('PASS selected native XLTX editor, no HTML grid, token reimport and automatic repeated rows');}finally{await browser.close();}})().catch(error=>{console.error(error);process.exitCode=1;});
