const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage();await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);
const result=await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=1000;c.height=800;await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'test.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[]});const a=ASMachApp,r=a.normalizeAnnotation({id:'q',number:'27',type:'Pah',nominalValue:'.1',unit:'mm',color:'#aa2222',shape:'square',page:1});a.state.annotations=[r];a.state.selectedId='q';a.renderAll();await ASMachRepeatedCharacteristics.setQuantity(a,'q',2);const list=a.state.annotations.map(r=>r.number),single=a.state.annotations.filter(r=>!r.listOnly).map(r=>ASMachRepeatedCharacteristics.drawingRecord(a.state,r).number);ASMachRepeatedCharacteristics.showBalloons(a,'q',true);const multi=a.state.annotations.filter(r=>!r.listOnly).map(r=>r.number),styles=a.state.annotations.map(r=>[r.color,r.shape]),positions=a.state.annotations.map(r=>r.bubbleY);const reloaded=a.state.annotations.map(a.normalizeAnnotation);ASMachRepeatedCharacteristics.showBalloons(a,'q',false);const after=a.state.annotations.map(r=>r.number);window.ASMachMessages.confirm=async()=>true;await ASMachRepeatedCharacteristics.setQuantity(a,'q',1);return{list,single,multi,styles,positions,reloaded:reloaded.map(r=>[r.number,r.repeatBaseNumber,r.showQuantityBalloons]),after,reduced:a.state.annotations.map(r=>r.number)};});
assert.deepEqual(result.list,['27.1','27.2']);assert.deepEqual(result.single,['27']);assert.deepEqual(result.multi,['27.1','27.2']);assert.deepEqual(result.styles,[['#aa2222','square'],['#aa2222','square']]);assert.notEqual(...result.positions);assert.equal(result.reloaded[0][1],'27');assert.equal(result.reloaded[0][2],true);assert.deepEqual(result.after,['27.1','27.2']);assert.deepEqual(result.reduced,['27']);console.log('PASS quantity numbering, single/multiple drawing labels, styles, distinct placement, persistence, reduction');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
