const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1900,height:1080}});await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=1600;c.height=1100;await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'layout.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[]});});await p.waitForTimeout(400);assert.ok((await p.locator('.workspace').boundingBox()).height>850);assert.ok((await p.locator('#viewer').boundingBox()).height>600);
await p.evaluate(()=>{const menu=document.createElement('details');menu.className='native-menu';menu.innerHTML='<summary>Windows</summary><section>Masaüstü testi</section>';window.ASMachDesktop={settingsPanel:menu};window.dispatchEvent(new Event('asmach-native-ready'));ASMachRibbon.open('view');});await p.locator('#desktopSettings [data-settings-tab=native]').click();assert.equal(await p.locator('#desktopSettings .native-menu section').isVisible(),true);assert.equal(await p.locator('.rb-tabs .native-menu').count(),0);await p.locator('#desktopSettings [data-settings-tab=appearance]').click();assert.equal(await p.locator('#desktopSettings .native-menu').isVisible(),false);console.log('PASS full-height drawing workspace and native settings tab');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
