const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),{pathToFileURL}=require('node:url'),path=require('node:path'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1600,height:1000}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.route(/^https?:/,r=>r.abort());await p.goto(pathToFileURL(path.resolve('ASMach_Teknik_Resim_Balonlama.html')).href);
 await p.evaluate(()=>{const a=ASMachApp;a.state.fileData='fixture';a.state.fileName='tabs.png';a.state.pageCount=1;a.state.currentPage=1;const c=a.elements.sourceCanvas;c.width=1000;c.height=700;c.getContext('2d').fillRect(0,0,1000,700);a.state.annotations=[a.normalizeAnnotation({id:'a',number:1,page:1,requirement:'20',nominalValue:'20',type:'Uzunluk',selectionBox:{x:.1,y:.1,w:.1,h:.1},inspectionMethod:a.getMethods()[0].value})];a.renderAll();});
 await p.locator('#balloonSettingsButton').click();await p.locator('[data-edit=color]').fill('#334455');await p.locator('#viewNumberingButton').click();await p.locator('.view-numbering-dialog [data-start]').fill('22');assert.equal(await p.locator('.view-numbering-dialog').evaluate(n=>n.matches(':modal')),false);
 await p.locator('#balloonSettingsButton').click();assert.equal(await p.locator('[data-edit=color]').inputValue(),'#334455');assert.equal(await p.locator('#settingsCancelButton').isVisible(),false);
 await p.locator('#viewNumberingButton').click();assert.equal(await p.locator('.view-numbering-dialog [data-start]').inputValue(),'22');assert.equal(await p.locator('.view-numbering-dialog [data-cancel]').isVisible(),false);
 await p.locator('[data-workspace-trigger=drawing]').click();assert.equal(await p.locator('.view-numbering-dialog').isVisible(),false);assert.equal(await p.locator('#balloonSettingsModal').isVisible(),false);assert.equal(await p.evaluate(()=>ASMachKeyboard.inWorkspace()),false);
 await p.locator('#viewNumberingButton').click();assert.equal(await p.locator('.view-numbering-dialog').count(),1);assert.equal(await p.locator('.view-numbering-dialog [data-start]').inputValue(),'22');
 const r=await p.locator('.view-numbering-dialog').boundingBox(),tab=await p.locator('.toolbar').boundingBox();assert.ok(Math.abs(r.y-tab.y-tab.height)<=1);
 const canvas=p.locator('.view-numbering-dialog [data-canvas]'),initial=await canvas.boundingBox();for(let i=0;i<5;i++)await p.locator('[data-zoom-in]').click();assert.ok((await canvas.boundingBox()).width>initial.width*2);
 assert.equal(await p.locator('.view-numbering-dialog .wf-tools').evaluate(n=>{const r=n.getBoundingClientRect();return n.contains(document.elementFromPoint(r.x+r.width/2,r.y+r.height/2));}),true,'Drawing overlay must not cover numbering tools');
 assert.equal(await p.locator('.vn-zoom-viewport').evaluate(n=>getComputedStyle(n).overflow),'auto');await p.locator('[data-fit]').click();assert.equal(await p.locator('[data-zoom-label]').textContent(),'100%');
 await p.locator('#balloonSettingsButton').click();await p.locator('#settingsSaveButton').click();assert.equal(await p.locator('#viewNumberingButton').count(),1);assert.equal(await p.locator('#balloonSettingsButton').getAttribute('aria-pressed'),'false');assert.deepEqual(errors,[]);await p.screenshot({path:'outputs/safety-review-qa/workspace-tabs.png'});console.log('PASS: nonmodal tabs, draft retention, single instances, drawing keyboard isolation, header offset and save lifecycle.');
}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
