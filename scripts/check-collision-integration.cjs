const fs=require('node:fs');let fixture=fs.readFileSync('scripts/check-characteristic-info.cjs','utf8').split('\n').slice(0,4).join('\n');
fixture+=`\nconst result=await p.evaluate(()=>{const app=ASMachApp,r=app.selectedAnnotation();const same={...r,id:'duplicate',number:'8'};app.state.annotations.push(same);const boxes=JSON.stringify(app.state.annotations.map(r=>r.selectionBox));app.renderAll();const a=app.state.annotations[0],b=app.state.annotations[1],distance=Math.hypot((a.bubbleX-b.bubbleX)*app.elements.sourceCanvas.width,(a.bubbleY-b.bubbleY)*app.elements.sourceCanvas.height);app.toast('Akıllı tarama: karakteristik eklendi. Yeni bir kutu seçebilirsiniz.');return{distance,boxesUnchanged:boxes===JSON.stringify(app.state.annotations.map(r=>r.selectionBox)),notices:[...document.querySelectorAll('.toast')].map(e=>e.textContent)};});assert.ok(result.distance>30);assert.ok(result.boxesUnchanged);assert.ok(!result.notices.some(t=>t.includes('Yeni bir kutu')));assert.deepEqual(errors,[]);await p.screenshot({path:'outputs/collision-placement.png'});console.log('PASS live separation, source boxes preserved and repeated success notice suppressed');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});`;eval(fixture);
