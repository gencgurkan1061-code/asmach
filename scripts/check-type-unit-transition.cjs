const fs=require('node:fs');let fixture=fs.readFileSync('scripts/check-characteristic-info.cjs','utf8').split('\n').slice(0,4).join('\n');fixture+=`
const results=await p.evaluate(async()=>{const out=[],api=ASMachCharacteristicEditing;window.ASMachMessages={...ASMachMessages,confirm:async()=>true,alert:async message=>{throw Error(message);}};const r=ASMachApp.selectedAnnotation();Object.assign(r,{type:'Uzunluk',unit:'mm',nominalValue:'63',lowerTolerance:'',upperTolerance:'',lowerLimit:'',upperLimit:'',requirementMode:'auto',manualFields:[]});ASMachApp.renderAll();for(const type of ['Yüzey','Açı','Çap','Not','GD&T']){await api.confirmCorrection(ASMachApp,r,{type});out.push([r.type,r.unit]);}Object.assign(r,{type:'Yüzey',unit:'µin'});ASMachApp.renderAll();ASMachApp.renderAll();out.push(['manual',r.unit]);out.push(['inch',api.defaultUnitForType('Çap','in')]);return out;});assert.deepEqual(results,[['Yüzey','µm'],['Açı','°'],['Çap','mm'],['Not',''],['GD&T','mm'],['manual','µin'],['inch','in']]);assert.deepEqual(errors,[]);console.log('PASS type changes select defaults, refresh preserves manual unit, imperial project respected');
}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});`;eval(fixture);
