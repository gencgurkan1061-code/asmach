const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1366,height:900}});await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=800;c.height=600;await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'test.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[{id:'r',number:'1',page:1,type:'Çap',nominalValue:'50',unit:'mm'}]});ASMachApp.state.selectedId='r';ASMachApp.renderAll();});
assert.deepEqual(await p.locator('#selectionForm .cw-tabs [role=tab]:visible').allTextContents(),['Genel','Denetleme','Balonlama']);assert.equal(await p.locator('#cwCalloutFields>summary').isVisible(),true);assert.equal(await p.locator('#cwInlineSource').isVisible(),false);await p.locator('#cwInspectorTab3').click();assert.equal(await p.locator('#cwInlineSource').isVisible(),true);assert.ok(await p.locator('#ipStylePane').evaluate(e=>e.firstElementChild.id==='cwInlineSource'));assert.equal(await p.locator('#cwCalloutFields>summary').isVisible(),false);await p.locator('#cwInspectorTab2').click();assert.equal(await p.locator('#cwInlineSource').isVisible(),false);await p.evaluate(()=>ASMachCharacteristicUI.openEditor('r'));assert.equal(await p.locator('#cwInspectorTab0').getAttribute('aria-selected'),'true');assert.equal(await p.locator('#acuiDetailsOpen').count(),0);console.log('PASS three tabs, source first under ballooning, special fields under general, detail routing');
await p.locator('#cwInspectorTab3').click();assert.equal(await p.locator('.cw-style-group').count(),5);await p.locator('.cw-style-group>summary').filter({hasText:'Balon görünümü'}).click();await p.locator('[data-local-style=shape]').selectOption('hexagon');assert.equal(await p.evaluate(()=>ASMachApp.state.annotations[0].shape),'hexagon');assert.equal(await p.locator('[data-local-style]').count(),20);console.log('PASS collapsible style groups and live shape editing');
await p.locator('#cwInspectorTab2').click();assert.equal(await p.locator('#ipControlPane .ct-tabs').isVisible(),false);assert.deepEqual((await p.locator('.ct-subgrid>summary').allTextContents()).map(s=>s.trim()),['Ortak','Operatör','Kalite']);for(const role of ['operator','quality']){await p.locator('[data-control-group='+role+']>summary').click();assert.ok(await p.locator('[data-control-group='+role+']').evaluate(e=>e.open));assert.equal(await p.locator('[data-control-group='+role+'] .rp-card').isVisible(),true);}console.log('PASS inspection subgrids with independently expanded operator and quality plans');
for(const tab of [0,2,3]){await p.locator('#cwInspectorTab'+tab).click();const weights=await p.locator('#selectionForm label:visible,#selectionForm summary:visible,#selectionForm .cw-field-label:visible,#selectionForm .cw-tabs [role=tab]:visible').evaluateAll(es=>es.map(e=>({label:e.textContent.trim().slice(0,35),weight:getComputedStyle(e).fontWeight})));assert.ok(weights.length);for(const item of weights)assert.equal(item.weight,'700',item.label);const values=await p.locator('#selectionForm input:not(#balloonNumber):visible,#selectionForm select:visible,#selectionForm textarea:visible').evaluateAll(es=>es.map(e=>getComputedStyle(e).fontWeight));assert.ok(values.every(w=>w==='400'));}console.log('PASS bold field/section/tab labels across all inspector tabs; values stay regular');
}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
