const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1366,height:768}});await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=1000;c.height=800;await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'t.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[]});document.getElementById('appSettingsButton').click();});const body=p.locator('#desktopSettings .ds-body');assert.ok(await body.evaluate(e=>e.scrollWidth<=e.clientWidth));assert.ok(await body.evaluate(e=>e.scrollHeight<=e.clientHeight));assert.equal(await p.locator('#desktopSettings [data-pref=density]').isVisible(),true);await p.screenshot({path:'outputs/settings-compact.png'});console.log('PASS appearance controls visible, no horizontal or vertical overflow at 1366x768');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
