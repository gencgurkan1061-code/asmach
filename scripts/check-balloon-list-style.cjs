const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),assert=require('node:assert/strict'),path=require('node:path'),{pathToFileURL}=require('node:url');
(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true});try{const page=await browser.newPage({viewport:{width:1500,height:900}}),errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(pathToFileURL(path.resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await page.evaluate(async()=>{const canvas=document.createElement('canvas');canvas.width=1000;canvas.height=700;const context=canvas.getContext('2d');context.fillStyle='white';context.fillRect(0,0,canvas.width,canvas.height);const original=ASMachRoleAppearance.effective;ASMachRoleAppearance.effective=(state,record)=>record.id==='style-sync'?{...record,shape:'hexagon',color:'#df3f4f',balloonFill:'#fff1f2',textColor:'#7f1d1d',balloonLineWidth:2,balloonLineStyle:'solid'}:original(state,record);await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'style-sync.png',type:'image/png',dataUrl:canvas.toDataURL()},annotations:[{id:'style-sync',number:'77.1',page:1,type:'GD&T',requirement:'⌖ | 0.1 | A | B | C',shape:'square',color:'#111827',size:20,fontSize:10,opacity:1,selectionBox:{x:.45,y:.4,w:.2,h:.05},anchorX:.45,anchorY:.425,bubbleX:.38,bubbleY:.425}]});ASMachApp.state.selectedId='style-sync';ASMachApp.renderAll();});const result=await page.evaluate(()=>{const drawing=document.querySelector('.balloon-group[data-id="style-sync"] .bubble-handle'),preview=document.querySelector('#balloonTableBody tr[data-id="style-sync"] .balloon-dot svg'),shape=preview?.firstElementChild;return{drawing:drawing?.tagName,drawStroke:drawing?.getAttribute('stroke'),list:shape?.tagName,listStroke:shape?.getAttribute('stroke'),dataShape:preview?.dataset.balloonShape,text:preview?.querySelector('text')?.textContent};});assert.deepEqual(result,{drawing:'polygon',drawStroke:'#df3f4f',list:'polygon',listStroke:'#df3f4f',dataShape:'hexagon',text:'77.1'});assert.deepEqual(errors,[]);console.log('PASS drawing and characteristic list use the same effective balloon shape and colors');}finally{await browser.close();}})().catch(error=>{console.error(error);process.exitCode=1;});
