const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),{pathToFileURL}=require('node:url'),path=require('node:path'),assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true});try{const p=await browser.newPage({viewport:{width:1500,height:950}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.route(/^https?:/,r=>r.abort());await p.goto(pathToFileURL(path.resolve('ASMach_Teknik_Resim_Balonlama.html')).href);
await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=1100;c.height=800;const g=c.getContext('2d');g.fillStyle='#fff';g.fillRect(0,0,1100,800);g.strokeRect(80,80,940,640);g.font='32px Arial';g.fillStyle='#234';g.fillText('Teknik resim · Önizleme testi',140,180);await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'preview.png',type:'image/png',dataUrl:c.toDataURL()},metadata:{partNo:'TEST-1'},annotations:[{id:'test1',number:'1',page:1,anchorX:.3,anchorY:.3,bubbleX:.4,bubbleY:.3,...ASMachApp.parseRequirement('20 ±0.1')}]});});
 await p.locator('#rbTab-drawing').click();assert.equal(await p.locator('#rbPanel-drawing #viewNumberingButton').count(),1);assert.equal(await p.locator('.toolbar #zoomInButton').count(),0);assert.equal(await p.locator('#drawingFloatingTools').isVisible(),true);
 const before=await p.locator('#zoomLabel').textContent();await p.locator('#zoomInButton').click();assert.notEqual(await p.locator('#zoomLabel').textContent(),before);await p.locator('#fitButton').click();
 const bounds=await p.locator('#drawingFloatingTools').boundingBox(),viewer=await p.locator('#viewer').boundingBox();assert.ok(bounds.x>=viewer.x&&bounds.x+bounds.width<=viewer.x+viewer.width+1);assert.ok(bounds.y>=viewer.y);await p.screenshot({path:'outputs/safety-review-qa/floating-drawing-tools.png'});
 await p.locator('#rbTab-view').click();assert.match(await p.locator('#rbTab-view').textContent(),/Ayarlar/);assert.equal(await p.locator('#desktopSettings').isVisible(),true);assert.equal(await p.locator('#drawingFloatingTools').isVisible(),false);assert.equal(await p.locator('#desktopSettings .ds-tabs [role=tab]').count(),5);
 await p.locator('#desktopSettings [data-settings-tab=drawing]').click();await p.locator('[data-pref=toolsPosition]').selectOption('bottom');await p.locator('[data-pref=background]').selectOption('dark');await p.locator('#desktopSettings [data-settings-tab=tables]').click();await p.locator('[data-pref=tableDensity]').selectOption('comfortable');await p.locator('[data-pref=tableText]').selectOption('large');assert.equal(await p.locator('.ds-sample td').first().evaluate(n=>getComputedStyle(n).fontSize),'14px');await p.locator('#desktopSettings [data-save]').click();assert.equal(await p.locator('#desktopSettings').isVisible(),true);assert.equal(await p.evaluate(()=>JSON.parse(localStorage.getItem('asmach.desktop.preferences.v1')).toolsPosition),'bottom');
 await p.screenshot({path:'outputs/safety-review-qa/settings-tabs.png'});await p.locator('[data-pref=tableText]').selectOption('normal');await p.locator('#desktopSettings .ds-actions [data-cancel]').click();assert.equal(await p.locator('[data-pref=tableText]').inputValue(),'large');
 await p.locator('#rbTab-drawing').click();assert.equal(await p.locator('#desktopSettings').isVisible(),false);assert.equal(await p.locator('#drawingFloatingTools').isVisible(),true);const bottom=await p.locator('#drawingFloatingTools').boundingBox();assert.ok(bottom.y>bounds.y+100);
 await p.locator('#viewNumberingButton').click();assert.equal(await p.locator('#rbTab-drawing').getAttribute('aria-selected'),'true');assert.equal(await p.locator('.view-numbering-dialog').isVisible(),true);assert.equal(await p.locator('#drawingFloatingTools').isVisible(),false);
 await p.locator('#rbTab-view').click();await p.locator('#desktopSettings [data-settings-tab=drawing]').click();await p.locator('#desktopSettings [data-settings-tab=styles]').click();assert.equal(await p.locator('#balloonSettingsModal').isVisible(),true);assert.equal(await p.locator('#rbTab-view').getAttribute('aria-selected'),'true');await p.locator('#balloonSettingsModal [data-edit=size][type=number]').fill('42');await p.locator('#balloonSettingsModal [data-edit=size][type=number]').press('Tab');await p.screenshot({path:'outputs/safety-review-qa/settings-balloon-styles.png'});await p.locator('#balloonSettingsModal .ds-style-tabs [data-settings-tab=drawing]').click();assert.equal(await p.locator('#desktopSettings').isVisible(),true);assert.equal(await p.locator('#desktopSettings [data-settings-tab=drawing]').getAttribute('aria-selected'),'true');await p.locator('#desktopSettings [data-settings-tab=styles]').click();assert.equal(await p.locator('#balloonSettingsModal [data-edit=size][type=number]').inputValue(),'42');
 await p.locator('#rbTab-view').click();await p.setViewportSize({width:650,height:900});assert.equal(await p.locator('#desktopSettings .ds-body').evaluate(n=>n.scrollWidth>n.clientWidth+2),false);await p.locator('#rbTab-drawing').click();const small=await p.locator('#drawingFloatingTools').boundingBox(),vsmall=await p.locator('#viewer').boundingBox();assert.ok(small.width<=vsmall.width);assert.deepEqual(errors,[]);console.log('PASS: numbering moved, floating zoom and fit, four settings tabs, live preferences, save/revert, workspace switching and narrow viewport.');
}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
