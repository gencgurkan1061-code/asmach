const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1400,height:950}});await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=800;c.height=600;await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'test.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[]});});await p.locator('#autoDetectButton').evaluate(e=>e.click());for(const value of ['rows','columns','clockwise']){await p.locator('#wfOrderTrigger').click();assert.ok(await p.locator('#wfOrderMenu').isVisible());await p.locator('#wfOrderMenu [data-order='+value+']').click();assert.equal(await p.locator('#wfNumberOrder').inputValue(),value);assert.equal(await p.evaluate(()=>ASMachApp.state.metadata.candidateNumberingOrder),value);assert.equal(await p.locator('#wfOrderTrigger svg').count(),1);assert.equal(await p.locator('#wfOrderTrigger svg').getAttribute('viewBox'),value==='clockwise'?'0 0 92 92':'0 0 24 24');}await p.locator('#wfOrderTrigger').focus();await p.keyboard.press('ArrowDown');await p.keyboard.press('Home');await p.keyboard.press('ArrowDown');await p.keyboard.press('Enter');assert.equal(await p.locator('#wfNumberOrder').inputValue(),'columns');await p.locator('#wfOrderTrigger').click();await p.keyboard.press('Escape');assert.equal(await p.locator('#wfOrderMenu').isVisible(),false);assert.equal(await p.locator('#wfNumberOrder').inputValue(),'columns');console.log('PASS supplied order SVGs, selection synchronization and arrow/Enter/Escape navigation');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
