const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),{pathToFileURL}=require('node:url'),path=require('node:path'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1600,height:1000}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.route(/^https?:/,r=>r.abort());await p.goto(pathToFileURL(path.resolve('ASMach_Teknik_Resim_Balonlama.html')).href);
 await p.evaluate(()=>{const a=ASMachApp;a.state.fileData='fixture';a.state.fileName='test.png';a.state.pageCount=1;const c=a.elements.sourceCanvas;c.width=1000;c.height=700;a.renderAll();});
 assert.equal(await p.locator('.qt-options').first().isVisible(),false);
 for(const target of ['#balloonSettingsButton','#appSettingsButton','#helpButton']){
  await p.locator('#boxOcrModeButton').click();assert.equal(await p.evaluate(()=>ASMachApp.state.mode),'box');
  await p.locator(target).click();assert.equal(await p.evaluate(()=>ASMachApp.state.mode),'select');assert.equal(await p.locator('.qt-options').first().isVisible(),false);assert.equal(await p.locator('#boxOcrModeButton').evaluate(n=>n.classList.contains('mode-active')),false);
  await p.locator('[data-workspace-trigger=drawing]').click();assert.equal(await p.locator('.qt-options').first().isVisible(),false);
 }
 await p.locator('#boxOcrModeButton').click();assert.equal(await p.locator('.qt-options').first().isVisible(),true);assert.equal(await p.locator('.qt-choice svg').count(),6);
 await p.locator('#balloonSettingsButton').click();await p.locator('#manualToolsButton').click();assert.equal(await p.locator('#balloonSettingsModal').isVisible(),false);assert.equal(await p.evaluate(()=>ASMachApp.state.mode),'add');assert.equal(await p.locator('.qt-options').first().isVisible(),false);assert.equal(await p.locator('#manualToolOptions').isVisible(),true);await p.locator('#selectModeButton').click();assert.equal(await p.locator('#manualToolOptions').isVisible(),false);await p.keyboard.press('b');assert.equal(await p.locator('#manualToolOptions').isVisible(),true);
 await p.locator('#appSettingsButton').click();assert.equal(await p.locator('#desktopSettings').evaluate(n=>n.matches(':modal')),false);await p.locator('#helpButton').click();assert.equal(await p.locator('#keyboardHelp').isVisible(),true);assert.equal(await p.locator('#desktopSettings').isVisible(),false);assert.equal(await p.locator('#keyboardHelp [data-chapter]').count(),12);await p.locator('#keyboardHelp [data-chapter="7"]').click();assert.match(await p.locator('#keyboardHelp main h1').textContent(),/numaralandırma/);assert.equal(await p.locator('#keyboardHelp svg').count(),1);await p.locator('#keyboardHelp input').fill('sertifika');assert.ok(await p.locator('#keyboardHelp [data-chapter]:visible').count()<12);
 await p.locator('#boxOcrModeButton').click();assert.equal(await p.locator('#keyboardHelp').isVisible(),false);assert.equal(await p.locator('.qt-options').first().isVisible(),true);await p.keyboard.press('F1');assert.equal(await p.locator('#keyboardHelp').isVisible(),true);assert.deepEqual(errors,[]);await p.screenshot({path:'outputs/safety-review-qa/usage-guide.png'});console.log('PASS: contextual OCR and icons, drawing routing, nonmodal settings/help, 12 guide chapters, search and F1.');
}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
