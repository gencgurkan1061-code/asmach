const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1500,height:950}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=1000;c.height=800;await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'t.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[{id:'a',number:'1',page:1,type:'Çap',requirement:'5',inspectionMethod:ASMachApp.getMethods()[0].value,selectionBox:{x:.4,y:.4,w:.1,h:.1},bubbleX:.3,bubbleY:.45}]});});await p.locator('#balloonSettingsButton').evaluate(e=>e.click());await p.locator('[data-edit=balloonFill]').fill('#123456');await p.locator('[data-edit=textColor]').fill('#ffffff');await p.locator('[data-edit=boxColor]').fill('#112233');await p.locator('[data-edit=leaderColor]').fill('#ff0000');assert.equal(await p.locator('[data-section="Balon"] [data-line-part=balloon]').count(),3);assert.equal(await p.locator('[data-section="Ölçü kutusu"] [data-line-part=box]').count(),4);await p.locator('[data-line-part=box][data-line-choice=none]').click();await p.locator('[data-edit=arrowEnabled]').check();await p.locator('[data-edit=arrowStyle]').selectOption('triangle');assert.equal(await p.locator('.appearance-group').count(),4);await p.locator('#settingsSaveButton').click();const state=await p.evaluate(()=>{const a=ASMachApp.state.annotations[0],g=document.querySelector('.balloon-group[data-id=a]');return {leader:g.querySelector('.leader').getAttribute('stroke'),boxColor:a.boxColor,fill:a.balloonFill,text:a.textColor,shapeFill:g.querySelector('.bubble-handle').getAttribute('fill'),arrow:!!g.querySelector('polygon[pointer-events=none]'),box:!!g.querySelector('.ocr-selection-outline'),normalized:ASMachApp.normalizeAnnotation(a).balloonFill};});assert.equal(state.leader,'#ff0000');assert.equal(state.boxColor,'#112233');assert.equal(state.fill,'#123456');assert.equal(state.text,'#ffffff');assert.equal(state.shapeFill,'#123456');assert.equal(state.arrow,true);assert.equal(state.box,false);assert.equal(state.normalized,'#123456');assert.deepEqual(errors,[]);console.log('PASS compact groups, fill/text settings, saved record normalization, hidden box with visible arrow and SVG fill');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1});
