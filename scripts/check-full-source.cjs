const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const {pathToFileURL}=require('node:url'),path=require('node:path'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1200,height:900}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.route(/^https?:/,r=>r.abort());await p.goto(pathToFileURL(path.resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(()=>{const h=document.createElement('div');h.id='qaFull';h.style.cssText='position:fixed;top:30px;left:30px;width:360px;background:white;z-index:99999';document.body.append(h);const c=document.createElement('canvas');c.width=1000;c.height=800;const g=c.getContext('2d');g.fillStyle='white';g.fillRect(0,0,1000,800);g.fillStyle='red';g.fillRect(0,0,20,20);g.fillStyle='blue';g.fillRect(980,780,20,20);g.fillStyle='black';g.font='30px Arial';g.fillText('20 ±0.1',430,410);ASMachCandidateSource.mount(h,{candidate:{pageImage:c,box:{x:.42,y:.45,w:.15,h:.08}},candidates:[],isCurrent:()=>true,recognize:async e=>{window.qaBox=e.box;return {...e,text:'20 ±0.1'};},apply:()=>{}});});const h=p.locator('#qaFull'),v=h.locator('.wf-source-viewport'),c=h.locator('canvas');assert.deepEqual(await c.evaluate(c=>[c.width,c.height]),[1000,800]);assert.notEqual(await h.locator('output').innerText(),'100%');assert.deepEqual(await c.evaluate(c=>Array.from(c.getContext('2d').getImageData(5,5,1,1).data)),[255,0,0,255]);await h.getByRole('button',{name:'Tüm sayfa',exact:true}).click();assert.equal(await h.locator('output').innerText(),'100%');await h.getByRole('button',{name:'Ölçü ve çevresi',exact:true}).click();const before=await v.evaluate(e=>e.scrollLeft),r=await v.boundingBox();await p.mouse.move(r.x+r.width/2,r.y+r.height/2);await p.mouse.down();await p.mouse.move(r.x+r.width/2-35,r.y+r.height/2-25,{steps:4});await p.mouse.up();assert.ok(await v.evaluate(e=>e.scrollLeft)>before);await h.getByRole('button',{name:'Tüm sayfa',exact:true}).click();await h.getByRole('button',{name:'Yeniden seç',exact:true}).click();const cr=await c.boundingBox();await p.mouse.move(cr.x+cr.width*.3,cr.y+cr.height*.3);await p.mouse.down();await p.mouse.move(cr.x+cr.width*.6,cr.y+cr.height*.6,{steps:4});await p.mouse.up();await p.waitForFunction(()=>!!window.qaBox);const box=await p.evaluate(()=>qaBox);assert.ok(Math.abs(box.x-.3)<.003&&Math.abs(box.w-.3)<.003);assert.deepEqual(await c.evaluate(c=>[c.width,c.height]),[1000,800]);assert.deepEqual(errors,[]);console.log('PASS: full-resolution full-page source retained, target zoom, fit, panning and page-space selection.');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
