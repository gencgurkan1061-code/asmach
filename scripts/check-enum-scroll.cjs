const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1366,height:768}});await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=1000;c.height=800;await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'t.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[]});ASMachApp.state.controlledDraft={source:ASMachApp.state.fileData,record:ASMachApp.normalizeAnnotation({id:'d',number:'1',type:'GD&T',requirement:'GD&T 0.03',page:1})};ASMachApp.renderAll();});await p.locator('button[aria-label="GD&T türü seç"]').click();const menu=p.locator('#gdtImageOptions');assert.equal(await menu.evaluate(m=>m.inert),false);const before=await p.locator('#selectionForm .ip-scroll').evaluate(e=>e.scrollTop);await menu.hover();await p.mouse.wheel(0,150);await p.waitForTimeout(150);assert.ok(await menu.evaluate(m=>m.scrollTop>0));await menu.evaluate(m=>m.scrollTop=m.scrollHeight);await p.mouse.wheel(0,500);await p.waitForTimeout(150);assert.equal(await p.locator('#selectionForm .ip-scroll').evaluate(e=>e.scrollTop),before);console.log('PASS enum scroll during controlled draft; no underlying panel scroll at boundary');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
