const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1500,height:950}});await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=1000;c.height=700;const g=c.getContext('2d');g.fillStyle='white';g.fillRect(0,0,1000,700);g.fillStyle='black';g.font='60px sans-serif';g.fillText('Ø25 ±0.1',100,120);const data=c.toDataURL();await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'test.png',type:'image/png',dataUrl:data},annotations:[],pendingCandidates:[20,30,40].map((n,i)=>({selected:false,page:1,box:{x:.1,y:.1,w:.2,h:.03},text:String(n),parsed:ASMachApp.parseRequirement(String(n)),snapshot:data,confidence:i===1?50:95,plan:{}}))});});await p.locator('#autoDetectButton').evaluate(e=>e.click());
assert.equal(await p.locator('[data-filter=all]').textContent(),'Tüm adaylar (3)');assert.equal(await p.locator('[data-filter=low]').textContent(),'Düşük güven (1)');await p.locator('[data-filter=low]').click();assert.equal(await p.locator('#wfRows tr[data-index]').count(),1);await p.locator('[data-filter=all]').click();
assert.equal(await p.locator('.wf-detail-title').isVisible(),false);assert.equal(await p.locator('.wf-review-source').count(),0);assert.equal(await p.locator('.wf-candidate-editor>#selectionForm').count(),1);const separator=p.getByRole('separator',{name:'Aday ayrıntıları panel genişliği'});const before=await p.locator('.wf-detail-pane').evaluate(e=>e.getBoundingClientRect().width);await separator.focus();await p.keyboard.press('ArrowLeft');assert.equal(await p.locator('.wf-detail-pane').evaluate(e=>e.getBoundingClientRect().width),before+20);
const grip=await separator.boundingBox();await p.mouse.move(grip.x+4,grip.y+50);await p.mouse.down();await p.mouse.move(grip.x-36,grip.y+50);await p.mouse.up();assert.ok(await p.locator('.wf-detail-pane').evaluate(e=>e.getBoundingClientRect().width)>before+20);
await p.getByRole('searchbox',{name:'Aday ara'}).fill('zzzz');assert.ok(await p.getByText('Bu filtrede aday yok',{exact:true}).isVisible());await p.getByRole('button',{name:'Filtreleri temizle',exact:true}).click();assert.equal(await p.locator('#wfRows tr[data-index]').count(),3);
await p.locator('#wfColumnPicker').click();assert.ok(await p.locator('#wfTableColumns').isVisible());await p.keyboard.press('Escape');await p.locator('#wfGrouping').selectOption('none');await p.locator('#wfRows tr[data-index="1"] [data-preview]').click();assert.ok(await p.locator('#wfRows tr[data-index="1"]').evaluate(e=>e.classList.contains('wf-active')));
await p.screenshot({path:'outputs/candidate-layout-preview.png'});
for(const width of [1000,1900]){await p.setViewportSize({width,height:950});await p.waitForTimeout(100);assert.ok(await p.locator('.wf-view-scan-row').evaluate(e=>e.scrollWidth<=e.clientWidth+1));assert.ok(await p.locator('.wf-review-grid').evaluate(e=>e.scrollWidth<=e.clientWidth+1));assert.equal(await p.locator('#wfAccept').count(),1);assert.equal(await p.locator('.wf-review-source').count(),0);assert.equal(await p.locator('.wf-candidate-editor>#selectionForm').count(),1);}
await p.locator('#wfClose').evaluate(e=>e.click());await p.evaluate(()=>{ASMachWorkflow.restoreQueue([]);ASMachApp.state.annotations.push(ASMachApp.normalizeAnnotation({id:'saved',number:'8',page:1,type:'Çap',nominalValue:'25',unit:'mm',requirement:'Ø25',autoDetected:true,selectionBox:{x:.1,y:.1,w:.2,h:.1}}));ASMachApp.renderAll();});await p.locator('#autoDetectButton').evaluate(e=>e.click());await p.getByRole('button',{name:'Eklenen 1 kaydı göster',exact:true}).click();assert.equal(await p.locator('#wfRows tr[data-index]').count(),1);assert.ok(await p.locator('#wfRows').getByText('#8',{exact:true}).isVisible());
console.log('PASS counted filters, empty/added-record state, clearing filters, pointer/keyboard resizing, grouping, column picker, selection and responsive layout');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
