const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const {pathToFileURL}=require('node:url'),path=require('node:path'),assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true});try{const p=await browser.newPage({viewport:{width:1400,height:950}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.route(/^https?:/,r=>r.abort());await p.goto(pathToFileURL(path.resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=900;c.height=600;const g=c.getContext('2d');g.fillStyle='white';g.fillRect(0,0,900,600);g.font='28px Arial';g.fillStyle='black';g.fillText('15.5 ±0.1',200,230);await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'compact.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[{id:'one',number:'46',page:1,...ASMachApp.parseRequirement('15.5 ±0.1'),ocrText:'15.5 ±0.1',selectionBox:{x:.22,y:.32,w:.18,h:.08},anchorX:.3,anchorY:.36,bubbleX:.44,bubbleY:.3}]});ASMachApp.state.selectedId='one';ASMachApp.renderAll();});await p.locator('.ci-source-editor canvas').waitFor();assert.equal(await p.locator('.ic-heading').count(),3);assert.equal(await p.locator('#selectionForm [role=tablist]').count(),0);await p.locator('#ipControlTab').click();assert.equal(await p.locator('#ipControlPane').isVisible(),true);assert.equal(await p.locator('#ipDataPane').isVisible(),true);await p.locator('#ciEditPlans').click();await p.locator('#ciPlanCancel').click();await p.locator('#ipControlTab').click();assert.equal(await p.locator('#ipControlPane').isVisible(),false);await p.locator('#ciOcrText').fill('16 ±0.2');await p.locator('#ciApplyOcr').click();assert.equal(await p.locator('#nominalValue').inputValue(),'16');const label=await p.locator('label[for=nominalValue]').boundingBox(),input=await p.locator('#nominalValue').boundingBox();assert.ok(Math.abs(label.y-input.y)<16);assert.ok(input.x>label.x);await p.locator('.ip-scroll').evaluate(el=>el.scrollTop=0);await p.screenshot({path:'outputs/safety-review-qa/inspector-compact.png'});await p.getByRole('button',{name:'Tümünü daralt',exact:true}).click();assert.equal(await p.locator('#selectionForm .ic-heading[aria-expanded=false]').count(),3);await p.getByRole('button',{name:'Tümünü aç',exact:true}).click();assert.equal(await p.locator('#selectionForm .ic-heading[aria-expanded=true]').count(),3);await p.locator('#ipStyleTab').click();await p.locator('#ipControlTab').click();assert.deepEqual(errors,[]);console.log('PASS compact inspector: independent accordion sections, live OCR bindings, inline labels, plan editing, expand/collapse all.');}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
