const fs=require('node:fs');let fixture=fs.readFileSync('scripts/check-characteristic-info.cjs','utf8').split('\n').slice(0,4).join('\n');
fixture+=`
for(const type of ['Çap','GD&T']){await p.evaluate(type=>{const r=ASMachApp.selectedAnnotation();Object.assign(r,ASMachApp.parseRequirement(type==='GD&T'?'∥ | 0.1 | A':'54.9 (0/-0.1)'));ASMachApp.renderAll();},type);const position=await p.evaluate(()=>{const body=document.querySelector('.cw-characteristic-body'),meta=body.querySelector('.cw-characteristic-meta'),evaluation=document.getElementById('evaluationMethod').closest('.field');return{children:[...body.children].map(n=>n.id||n.className),last:body.lastElementChild===meta,after:!!(evaluation.compareDocumentPosition(meta)&Node.DOCUMENT_POSITION_FOLLOWING),counts:['balloonPage','balloonZone','toleranceStandard'].map(id=>document.querySelectorAll('#'+id).length)};});console.log(type,position);assert.ok(position.last&&position.after);assert.deepEqual(position.counts,[1,1,1]);}await p.screenshot({path:'outputs/characteristic-metadata-bottom.png'});assert.deepEqual(errors,[]);console.log('PASS page/zone/tolerance source below evaluation, stable after type changes, no duplicate fields');
}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});`;eval(fixture);
