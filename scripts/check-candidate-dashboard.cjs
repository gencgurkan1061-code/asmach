const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const {pathToFileURL}=require('node:url'),path=require('node:path'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1366,height:900}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.route(/^https?:/,r=>r.abort());await p.goto(pathToFileURL(path.resolve('ASMach_Teknik_Resim_Balonlama.html')).href);
await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=1000;c.height=700;const g=c.getContext('2d');g.fillStyle='white';g.fillRect(0,0,1000,700);g.fillStyle='black';g.font='28px Arial';g.fillText('20 ±0.1',100,100);const source=c.toDataURL();await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'qa.png',type:'image/png',dataUrl:source},annotations:[],pendingCandidates:Array.from({length:12},(_,i)=>({selected:i===0,page:1,box:{x:.1,y:.1+i*.04,w:.2,h:.03},text:'20 ±0.1',parsed:ASMachApp.parseRequirement('20 ±0.1'),snapshot:source,confidence:i===1?50:95,plan:{inspectionMethod:ASMachApp.getMethods()[0].value,inspectionFrequency:i===2?'':'ALL'}}))});});
await p.evaluate(()=>{const rows=ASMachWorkflow.exportQueue();rows.forEach((c,i)=>c.scanRegion={id:i<6?'area-a':'area-b',page:1,box:{x:0,y:i<6?0:.5,w:1,h:.5}});ASMachWorkflow.restoreQueue(rows);});
await p.locator('#autoDetectButton').click();await p.locator('.wf-dashboard.open').waitFor();assert.equal(await p.locator('#wfRows tr[data-index]').count(),12);assert.equal(await p.locator('#wfRows select').count(),0);assert.ok(await p.locator('.wf-candidate-editor select').count()>0);assert.equal(await p.locator('.wf-area-card img').count(),2);assert.equal(await p.locator('.ct-group').count(),2);await p.locator('[data-area="area-b"]').click();assert.equal(await p.locator('#wfRows tr[data-index]:visible').count(),6);await p.locator('.wf-area-strip').getByText('Tüm alanlar',{exact:true}).click();assert.equal(await p.evaluate(()=>ASMachWorkflow.exportQueue()[0].scanRegion.id),'area-a');
await p.locator('#wfGrouping').selectOption('type');assert.equal(await p.locator('.ct-group').count(),1);await p.locator('#wfGrouping').selectOption('view-type');assert.equal(await p.locator('.ct-group').count(),4);await p.locator('#wfGrouping').selectOption('none');assert.equal(await p.locator('.ct-group').count(),0);await p.locator('#wfGrouping').selectOption('view');const full=await p.locator('.wf-card').boundingBox();assert.equal(full.width,1366);assert.equal(full.height,900);assert.equal(full.x,0);assert.equal(full.y,0);
await p.locator('#wfColumnPicker').click();await p.locator('#wfTableColumns').getByText('Nominal',{exact:true}).click();assert.equal(await p.locator('.wf-table th[data-column="nominalValue"]').count(),1);await p.keyboard.press('Escape');
await p.locator('#wfRows tr[data-index="1"] td[data-column="requirement"]').click();assert.match(await p.locator('.wf-candidate-editor strong').innerText(),/Aday 2/);
await p.locator('[data-filter="low"]').click();assert.equal(await p.locator('#wfRows tr[data-index]:visible').count(),1);await p.locator('[data-filter="all"]').click();
await p.locator('.wf-candidate-editor input[aria-label="Algılanan metin"]').fill('21 ±0.1');await p.locator('.wf-candidate-editor input[aria-label="Algılanan metin"]').press('Tab');assert.equal(await p.evaluate(()=>ASMachWorkflow.exportQueue()[1].text),'21 ±0.1');
await p.locator('[data-review-field="nominalValue"]').fill('22');await p.locator('[data-review-field="nominalValue"]').press('Tab');assert.equal(await p.locator('[data-review-field="lowerLimit"]').inputValue(),'21.9');
await p.locator('.wf-bulk summary').click();await p.locator('#wfBulkType').selectOption('Çap');await p.locator('#wfFrequency').selectOption('CUSTOM');const before=await p.evaluate(()=>JSON.stringify(ASMachWorkflow.exportQueue()));await p.locator('#wfPlanSelected').click();assert.equal(await p.evaluate(()=>JSON.stringify(ASMachWorkflow.exportQueue())),before,'Invalid bulk update must be atomic');await p.locator('#wfBulkType').selectOption('');await p.locator('#wfFrequency').selectOption('EACH_PART');await p.locator('#wfPlanSelected').click();assert.equal(await p.evaluate(()=>ASMachWorkflow.exportQueue()[0].plan.inspectionFrequency),'EACH_PART');assert.equal(await p.evaluate(()=>ASMachWorkflow.exportQueue()[1].plan.inspectionFrequency),'ALL');await p.screenshot({path:'outputs/safety-review-qa/candidate-bulk.png'});await p.locator('.wf-bulk summary').click();
await p.screenshot({path:'outputs/safety-review-qa/candidate-dashboard.png'});await p.locator('#wfClose').click();await p.locator('#autoDetectButton').click();assert.equal(await p.locator('#wfRows tr[data-index]').count(),12);await p.locator('#wfAccept').click();assert.equal(await p.evaluate(()=>ASMachApp.state.annotations.length),1);assert.equal(await p.locator('#wfRows tr[data-index]').count(),11);await p.locator('#wfAccept').evaluate(e=>e.click());assert.equal(await p.evaluate(()=>ASMachApp.state.annotations.length),1);assert.deepEqual(errors,[]);console.log('PASS: queue, editor, filters, tolerance limits, selected-only plans, reopen and duplicate-free acceptance.');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
