const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const {pathToFileURL}=require('node:url'),path=require('node:path'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1920,height:900}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.route(/^https?:/,r=>r.abort());await p.goto(pathToFileURL(path.resolve('ASMach_Teknik_Resim_Balonlama.html')).href);
 await p.locator('#balloonSettingsButton').click();const picks=p.locator('.appearance-nav button');await picks.first().focus();await p.keyboard.press('ArrowDown');assert.equal(await picks.nth(1).evaluate(n=>document.activeElement===n),true);await p.keyboard.press('Enter');assert.equal(await picks.nth(1).getAttribute('aria-pressed'),'true');await p.keyboard.press('End');assert.equal(await picks.last().evaluate(n=>document.activeElement===n),true);
 const name=p.locator('[data-edit=label]');await name.fill('Test adı');await name.press('Home');await name.press('ArrowRight');assert.equal(await name.evaluate(n=>n.selectionStart),1);assert.equal(await name.evaluate(n=>document.activeElement===n),true);
 await p.locator('#settingsCancelButton').click();await p.keyboard.press('F1');assert.equal(await p.locator('#keyboardHelp').evaluate(n=>n.open),true);await p.keyboard.press('Escape');assert.equal(await p.locator('#keyboardHelp').evaluate(n=>n.open),false);
 await p.evaluate(()=>ASMachReportCenter.open());const cards=p.locator('.rc-card');await cards.first().focus();await p.keyboard.press('ArrowRight');const mode=await p.evaluate(()=>ASMachApp.state.mode);await p.keyboard.press('b');assert.equal(await p.evaluate(()=>ASMachApp.state.mode),mode);await p.locator('#reportCenter [data-close]').focus();await p.keyboard.press('Alt+ArrowLeft');assert.equal(await p.locator('#reportCenter').count(),0);
 assert.deepEqual(errors,[]);console.log('PASS: arrow focus, Enter activation, Home/End, text caret preserved, F1/Esc, workspace isolation and back shortcut.');
}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
