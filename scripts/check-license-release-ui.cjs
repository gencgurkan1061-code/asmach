// Only temporary generated keys/data; remote requests are blocked.
const fs=require('node:fs'),path=require('node:path'),os=require('node:os'),crypto=require('node:crypto'),assert=require('node:assert/strict');
const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const {start}=require('../license-manager/server.cjs');
(async()=>{const root=fs.mkdtempSync(path.join(os.tmpdir(),'asmach-release-ui-'));let app,browser;try{
 fs.mkdirSync(path.join(root,'.license-admin'));fs.mkdirSync(path.join(root,'src-tauri'));const pair=crypto.generateKeyPairSync('ed25519');fs.writeFileSync(path.join(root,'.license-admin/license.pem'),pair.privateKey.export({type:'pkcs8',format:'pem'}));fs.writeFileSync(path.join(root,'src-tauri/license-config.json'),JSON.stringify({licensePublicKey:Buffer.from(pair.publicKey.export({format:'jwk'}).x,'base64url').toString('base64')}));
 app=await start(root);browser=await chromium.launch({channel:'msedge',headless:true});const page=await browser.newPage({viewport:{width:1280,height:850}}),errors=[];page.on('pageerror',e=>errors.push(e.message));await page.route('**/*',r=>new URL(r.request().url()).origin===app.origin?r.continue():r.abort());await page.goto(app.url);await page.getByRole('button',{name:'Sürümler',exact:true}).click();
 const bytes=Buffer.alloc(1024);bytes.write('MZ');bytes.writeUInt32LE(64,60);bytes.write('PE\0\0',64);bytes.writeUInt16LE(0x14c,68);
 await page.locator('.release-form [name=version]').fill('2.9.0');await page.locator('.release-form [name=file]').setInputFiles({name:'test-setup.exe',mimeType:'application/octet-stream',buffer:bytes});await page.getByRole('button',{name:'Dosyayı al ve kontrol et'}).click();await page.locator('.release-item').waitFor();assert.equal(await page.getByRole('button',{name:'Kontrol et ve yayınla'}).isDisabled(),true);await page.screenshot({path:path.resolve('outputs/license-release-center.png'),fullPage:true});await page.setViewportSize({width:760,height:900});assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),true);assert.deepEqual(errors,[]);console.log('PASS release upload UI, draft creation, disabled unconfigured publishing, narrow layout');
 }finally{if(browser)await browser.close();if(app)await new Promise(r=>app.server.close(r));fs.rmSync(root,{recursive:true,force:true});}})().catch(e=>{console.error(e);process.exitCode=1});
