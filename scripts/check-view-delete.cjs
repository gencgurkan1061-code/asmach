const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1500,height:950}});await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=1000;c.height=700;const data=c.toDataURL();await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'test.png',type:'image/png',dataUrl:data},annotations:[],pendingCandidates:[{selected:true,page:1,box:{x:.1,y:.1,w:.2,h:.03},text:'20',parsed:ASMachApp.parseRequirement('20'),snapshot:data,confidence:95,plan:{}}]});ASMachViewNumbering.registerScanArea(ASMachApp,{page:1,name:'Test görünüş',box:{x:0,y:0,w:.8,h:.8}});ASMachMessages.confirm=async()=>true;});await p.locator('#autoDetectButton').evaluate(e=>e.click());assert.equal(await p.locator('.wf-area-card[data-area]').count(),1);assert.equal(await p.locator('#wfRows tr[data-index]').count(),1);await p.locator('.wf-area-delete').click();assert.equal(await p.locator('.wf-area-card[data-area]').count(),0);assert.equal(await p.locator('#wfRows tr[data-index]').count(),1);assert.equal(await p.locator('.wf-area-strip').isVisible(),false);assert.match(await p.locator('#wfRows').textContent(),/Görünüş atanmamış/);await p.locator('#wfClose').evaluate(e=>e.click());await p.locator('#autoDetectButton').evaluate(e=>e.click());assert.equal(await p.locator('.wf-area-card[data-area]').count(),0);assert.equal(await p.locator('#wfRows tr[data-index]').count(),1);console.log('PASS deleted view stays removed across reopening; candidate remains in unassigned group');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
