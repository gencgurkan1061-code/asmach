const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1500,height:950}});await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=1000;c.height=800;await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'t.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[{id:'a',number:'1',type:'Çap',nominalValue:'25',unit:'mm',page:1},{id:'b',number:'2',type:'Uzunluk',nominalValue:'10',unit:'mm',page:1}]});ASMachApp.state.selectedId='a';ASMachApp.renderAll();});
await p.locator('#nominalValue').focus();await p.locator('#overlay').click({position:{x:20,y:20}});await p.keyboard.press('Control+a');assert.deepEqual(await p.evaluate(()=>ASMachCharacteristicUI.selectionIds().sort()),['a','b']);
await p.evaluate(()=>{ASMachCharacteristicUI.selectIds([]);ASMachApp.state.selectedId='a';ASMachApp.renderAll();});await p.locator('#nominalValue').focus();await p.keyboard.press('Control+a');assert.deepEqual(await p.evaluate(()=>ASMachCharacteristicUI.selectionIds()),[]);assert.equal(await p.locator('#nominalValue').evaluate(e=>e.selectionEnd-e.selectionStart),2);
await p.locator('#overlay').click({position:{x:20,y:20}});await p.evaluate(()=>{void ASMachMessages.confirm('Test');});await p.keyboard.press('Control+a');assert.deepEqual(await p.evaluate(()=>ASMachCharacteristicUI.selectionIds()),[]);await p.keyboard.press('Escape');console.log('PASS drawing click focuses PDF area, Ctrl+A selects all balloons, text editing and dialog scope preserved');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
