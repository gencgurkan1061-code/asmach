const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const {pathToFileURL}=require('node:url'),path=require('node:path'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1400,height:900}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.route(/^https?:/,r=>r.abort());await p.goto(pathToFileURL(path.resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=400;c.height=300;await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'modal.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[]});ASMachProduction.open('components');});const ui=p.locator('#productionWorkspace'),d=ui.locator('dialog[open]'),f=k=>d.locator(`[data-field="${k}"]`);await ui.locator('[data-add]').click();await f('name').fill('Gövde');await d.locator('[data-save]').click();assert.match(await d.locator('[data-modal-status]').innerText(),/Parça numarası/);await f('partNo').fill('P-1');await f('lot').fill('LOT-1');await f('certificateNo').fill('CERT');await d.locator('[data-save]').click();assert.equal(await d.count(),0);assert.equal(await ui.locator('[data-row]').count(),1);await ui.locator('[data-row-copy]').click();assert.equal(await f('lot').inputValue(),'');assert.equal(await f('certificateNo').inputValue(),'');assert.equal(await f('partNo').inputValue(),'P-1');assert.equal(await ui.locator('[data-row]').count(),1);await d.locator('[data-save]').click();assert.equal(await ui.locator('[data-row]').count(),2);assert.equal(await p.evaluate(()=>new Set(ASMachProduction.data(ASMachApp.state).components.map(r=>r.id)).size),2);await ui.locator('[data-row-edit]').first().click();await f('name').fill('Changed');await d.locator('[data-cancel]').click();await p.locator('.pr-confirm [data-no]').click();assert.equal(await f('name').inputValue(),'Changed');await d.locator('[data-save]').click();assert.equal(await p.evaluate(()=>ASMachProduction.data(ASMachApp.state).components[0].name),'Changed');await ui.locator('[data-row]').first().dblclick();await f('name').fill('Discard');await p.keyboard.press('Escape');await p.locator('.pr-confirm [data-yes]').click();await d.waitFor({state:'detached'});assert.equal(await d.count(),0);assert.equal(await p.evaluate(()=>ASMachProduction.data(ASMachApp.state).components[0].name),'Changed');await ui.locator('[data-add]').click();await p.screenshot({path:'outputs/safety-review-qa/production-modal.png'});await d.locator('[data-cancel]').click();for(const tab of ['records','operations','controls','evidence']){await ui.locator(`[data-tab="${tab}"]`).click();await ui.locator('[data-add]').click();assert.equal(await d.count(),1);await d.locator('[data-cancel]').click();}assert.deepEqual(errors,[]);console.log('PASS production modals: validation, creation, independent safe copy, edit, double-click, discard confirmation, all table forms.');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
