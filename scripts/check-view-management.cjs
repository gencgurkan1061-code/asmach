const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const {pathToFileURL}=require('node:url'),path=require('node:path'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1366,height:900}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.route(/^https?:/,r=>r.abort());await p.goto(pathToFileURL(path.resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=1000;c.height=700;await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'view.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[]});window.ocrCalls=0;ASMachOCR.detect=async()=>{ocrCalls++;return{words:[]};};});
await p.locator('#autoDetectButton').click();assert.equal(await p.evaluate(()=>!!ASMachApp.state.autoScanRequest),false);await p.locator('#wfReselect').click();await p.locator('#selectModeButton').click();await p.waitForTimeout(100);assert.equal(await p.locator('#automaticReviewModal').isVisible(),false);assert.equal(await p.evaluate(()=>!!ASMachApp.state.autoScanRequest),false);
await p.locator('#autoDetectButton').click();
for(const method of ['button','escape','drag-escape']){const before=await p.evaluate(()=>JSON.stringify({views:ASMachApp.state.metadata.viewNumbering,queue:ASMachWorkflow.exportQueue()}));await p.locator('#wfReselect').click();await p.locator('#scanAreaReturnButton').waitFor();if(method==='button')await p.locator('#scanAreaReturnButton').click();else{if(method==='drag-escape'){const r=await p.locator('#overlay').boundingBox();await p.mouse.move(r.x+r.width*.2,r.y+r.height*.2);await p.mouse.down();await p.mouse.move(r.x+r.width*.4,r.y+r.height*.4);}await p.keyboard.press('Escape');if(method==='drag-escape')await p.mouse.up();}await p.locator('#automaticReviewModal.open').waitFor();assert.equal(await p.locator('#scanAreaReturnBar').count(),0);assert.equal(await p.evaluate(()=>!!ASMachApp.state.autoScanRequest||!!ASMachApp.state.boxDrag),false);assert.equal(await p.evaluate(()=>JSON.stringify({views:ASMachApp.state.metadata.viewNumbering,queue:ASMachWorkflow.exportQueue()})),before);}
await p.locator('#wfReselect').click();const r=await p.locator('#overlay').boundingBox();await p.mouse.move(r.x+r.width*.2,r.y+r.height*.2);await p.mouse.down();await p.mouse.move(r.x+r.width*.7,r.y+r.height*.7,{steps:5});await p.mouse.up();await p.locator('#automaticReviewModal.open').waitFor();assert.equal(await p.evaluate(()=>ocrCalls),0);assert.equal(await p.evaluate(()=>ASMachApp.state.metadata.viewNumbering.views.length),1);assert.equal(await p.evaluate(()=>ASMachWorkflow.exportQueue().length),0);
await p.evaluate(()=>{ASMachApp.state.annotations.push(ASMachApp.normalizeAnnotation({page:1,number:'1',type:'Uzunluk',anchorX:.4,anchorY:.4,nominalValue:'10'}));});p.once('dialog',async d=>{assert.match(d.message(),/1 kayıtlı ölçü/);await d.dismiss();});await p.locator('.wf-area-delete').click();assert.equal(await p.evaluate(()=>ASMachApp.state.metadata.viewNumbering.views.length),1);p.once('dialog',d=>d.accept());await p.locator('.wf-area-delete').click();assert.equal(await p.evaluate(()=>ASMachApp.state.metadata.viewNumbering.views.length),0);assert.equal(await p.evaluate(()=>ASMachApp.state.annotations.length),1);await p.locator('#wfClose').click();await p.locator('#autoDetectButton').click();assert.equal(await p.evaluate(()=>ASMachApp.state.metadata.viewNumbering.views.length),0);assert.deepEqual(errors,[]);console.log('PASS: explicit view creation without OCR, normal selection cancels without redirect, delete warning/cancel, measurements preserved, no resurrection.');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
