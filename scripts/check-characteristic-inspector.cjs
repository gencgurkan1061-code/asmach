const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const {pathToFileURL}=require('node:url'),path=require('node:path'),assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true});try{
 const p=await browser.newPage({viewport:{width:1600,height:1000}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.route(/^https?:/,r=>r.abort());await p.goto(pathToFileURL(path.resolve('ASMach_Teknik_Resim_Balonlama.html')).href);
 await p.evaluate(async()=>{const canvas=document.createElement('canvas');canvas.width=1000;canvas.height=700;const g=canvas.getContext('2d');g.fillStyle='white';g.fillRect(0,0,1000,700);g.fillStyle='black';g.font='24px Arial';g.fillText('20 ±0.1',300,300);g.fillText('Ø27.3',300,370);const source=canvas.toDataURL();await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'inspector.png',type:'image/png',dataUrl:source},annotations:[0,1].map(i=>({id:'record'+i,number:String(i+1),page:1,selectionBox:{x:.3,y:.39+i*.15,w:.13,h:.05},anchorX:.36,anchorY:.415+i*.15,bubbleX:.46,bubbleY:.36+i*.15,ocrText:'20 ±0.1',...ASMachApp.parseRequirement('20 ±0.1'),requirement:'Özel üretim açıklaması',requirementMode:'manual',manualFields:['requirement'],snapshot:source,inspectionMethod:ASMachApp.getMethods()[0].value,inspectionFrequency:'EACH_PART',rolePlans:{operator:{...ASMachRolePlans.blank(),enabled:true,inspectionMethod:ASMachApp.getMethods()[0].value,inspectionFrequency:'EACH_PART',sampleMode:'all'}}}))});ASMachApp.state.selectedId='record0';ASMachApp.renderAll();window.ASMachAutomaticGeometry={...ASMachAutomaticGeometry,place:()=>true};window.ASMachOCR={...ASMachOCR,recognize:async()=>({text:'Ø27.3 (0/-0.05)',confidence:97,source:'QA'})};});
 await p.locator('.ci-source-editor canvas').waitFor();assert.equal(await p.locator('h2').filter({hasText:'Karakteristik özelliği'}).count(),1);
 await p.locator('#balloonNumber').fill('2');await p.locator('#balloonNumber').press('Tab');assert.equal(await p.evaluate(()=>ASMachApp.state.annotations.find(r=>r.id==='record0').number),'1');assert.match(await p.locator('#ciNumberHint').innerText(),/kullanımda/);await p.locator('#ciNumberApply').click();await p.getByRole('button',{name:/Numaraları değiş tokuş et/}).click();await p.waitForFunction(()=>ASMachApp.state.annotations.find(r=>r.id==='record0').number==='2');assert.deepEqual(await p.evaluate(()=>['record0','record1'].map(id=>ASMachApp.state.annotations.find(r=>r.id===id).number)),['2','1']);
 await p.locator('#balloonNumber').fill('');await p.locator('#balloonNumber').press('Tab');assert.equal(await p.locator('#ciNumberApply').isDisabled(),true);await p.locator('#ciNumberFree').click();assert.equal(await p.locator('#balloonNumber').inputValue(),'3');await p.locator('#balloonNumber').press('Escape');assert.equal(await p.locator('#balloonNumber').inputValue(),'2');
 await p.locator('#ciOcrText').fill('Ø27.3 (0/-0.05)');await p.locator('#ciApplyOcr').click();assert.equal(await p.locator('#nominalValue').inputValue(),'27.3');assert.equal(await p.locator('#lowerTolerance').inputValue(),'-0.05');assert.equal(await p.locator('#requirement').inputValue(),'Özel üretim açıklaması');assert.equal(await p.evaluate(()=>ASMachApp.state.annotations.length),2);
 await p.locator('#ipControlTab').click();await p.locator('#ciEditPlans').click();await p.locator('.ci-plan-editor [data-role=operator][data-key=frequencyNote]').fill('Vardiya kontrolü');await p.locator('.ci-plan-editor [data-role=operator][data-key=frequencyNote]').press('Tab');assert.notEqual(await p.evaluate(()=>ASMachApp.state.annotations.find(r=>r.id==='record0').rolePlans.operator.frequencyNote),'Vardiya kontrolü');await p.locator('#ciPlanSave').click();assert.equal(await p.evaluate(()=>ASMachApp.state.annotations.find(r=>r.id==='record0').rolePlans.operator.frequencyNote),'Vardiya kontrolü');await p.screenshot({path:'outputs/safety-review-qa/characteristic-inspection.png'});await p.locator('#ipDataTab').click();
 const source=p.locator('.ci-source-editor');const stable=await p.evaluate(()=>JSON.stringify(ASMachApp.state.annotations));await source.getByRole('button',{name:'Yeniden seç',exact:true}).click();assert.equal(await p.evaluate(()=>ASMachApp.state.inlineReselectionId),'record0');assert.equal(await p.evaluate(()=>JSON.stringify(ASMachApp.state.annotations)),stable);await p.keyboard.press('Escape');assert.equal(await p.evaluate(()=>ASMachApp.state.inlineReselectionId),null);assert.equal(await p.evaluate(()=>JSON.stringify(ASMachApp.state.annotations)),stable);
 async function selectBox(){await source.getByRole('button',{name:'Yeniden seç',exact:true}).click();const r=await source.locator('canvas').boundingBox();await p.mouse.move(r.x+r.width*.22,r.y+r.height*.23);await p.mouse.down();await p.mouse.move(r.x+r.width*.85,r.y+r.height*.8,{steps:5});await p.mouse.up();}
 await selectBox();await p.waitForFunction(()=>document.querySelector('.ci-source-editor .wf-source-draft input').value==='Ø27.3 (0/-0.05)');await source.getByRole('textbox',{name:'Yeni seçim metni'}).fill('Ø28 (0/-0.05)');await source.getByRole('button',{name:'Uygula',exact:true}).click();assert.equal(await p.locator('#nominalValue').inputValue(),'28');assert.equal(await p.locator('#requirement').inputValue(),'Özel üretim açıklaması');assert.equal(await p.locator('#balloonNumber').inputValue(),'2');assert.equal(await p.evaluate(()=>ASMachApp.state.annotations.find(r=>r.id==='record0').rolePlans.operator.frequencyNote),'Vardiya kontrolü');assert.notDeepEqual(await p.evaluate(()=>ASMachApp.state.annotations.find(r=>r.id==='record0').selectionBox),JSON.parse(stable).find(r=>r.id==='record0').selectionBox);assert.equal(await p.evaluate(()=>ASMachApp.state.inlineReselectionId),null);assert.equal(await p.evaluate(()=>ASMachApp.state.annotations.length),2);
 await p.evaluate(()=>{window.ASMachOCR={...ASMachOCR,recognize:()=>new Promise(resolve=>window.finishInspectorRead=resolve)};});await selectBox();await p.waitForFunction(()=>!!window.finishInspectorRead);await p.evaluate(()=>{ASMachApp.state.selectedId='record1';ASMachApp.renderAll();finishInspectorRead({text:'99',confidence:100});});await p.locator('.ci-source-editor canvas').waitFor();assert.equal(await p.locator('#nominalValue').inputValue(),'20');assert.equal(await p.evaluate(()=>ASMachApp.state.inlineReselectionId),null);await p.screenshot({path:'outputs/safety-review-qa/characteristic-inspector.png'});assert.deepEqual(errors,[]);
 console.log('PASS: explicit number apply/conflict/free/cancel, independent OCR and description, inline role plan, hidden old marker, cancel preservation, source apply and stale OCR safety.');
}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
