const {chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage();await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);const result=await p.evaluate(()=>{const t=ASMachFai837Template.preset(),state={metadata:{partNo:'PART-TEST'},annotations:[{number:'7',requirement:'25 ±0.1',result:'25.02',inspectionMethod:'KUMPAS',comment:'Kontrol',page:1}]};const f=ASMachFai837Template.files(t,state,false);let guarded=false;try{ASMachFai837Template.files(t,{...state,annotations:Array(32).fill(state.annotations[0])},false);}catch{guarded=true;}ASMachExcelCellTemplate.open(ASMachApp);return {first:f['xl/worksheets/sheet1.xml'],third:f['xl/worksheets/sheet3.xml'],guarded};});assert.ok(result.first.includes('PART-TEST'));assert.ok(result.third.includes('25 ±0.1'));assert.ok(result.third.includes('25.02'));assert.ok(result.guarded);await p.locator('#cellTemplateEditor [data-library]').selectOption('F-837-001 · İlk Parça Onay FAI');assert.ok(await p.locator('[data-cell="7:14"]').isVisible());assert.equal(await p.locator('#cellTemplateEditor table tr').count(),39);console.log('PASS original-form fill, mappings and overflow guard');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
