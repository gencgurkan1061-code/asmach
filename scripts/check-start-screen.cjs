const{chromium}=require('C:/Users/gencg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:1366,height:900}});await p.goto(require('url').pathToFileURL(require('path').resolve('ASMach_Teknik_Resim_Balonlama.html')).href);await p.locator('#drawingHome').waitFor();assert.equal(await p.locator('#startScreen').count(),0);assert.equal(await p.locator('#viewer #drawingHome').count(),1);assert.ok(await p.locator('[data-home-drawing]').isVisible());assert.ok(await p.locator('[data-home-project]').isVisible());
await p.evaluate(async()=>{const c=document.createElement('canvas');c.width=500;c.height=400;await ASMachApp.restoreProject({format:'asmach-ballooning-project',source:{name:'Son çalışma.png',type:'image/png',dataUrl:c.toDataURL()},annotations:[]});await ASMachStartScreen.remember(ASMachApp.projectPayload(),{saved:true,name:'Kaydedilen proje.balloon.json'});});await p.locator('#drawingHome').waitFor({state:'hidden'});
await p.reload();await p.locator('[data-saved] .home-recent').waitFor();assert.equal(await p.locator('[data-saved] .home-recent').count(),1);assert.equal(await p.locator('[data-recent] .home-recent').count(),1);await p.screenshot({path:'outputs/drawing-home.png'});await p.locator('[data-saved] .home-recent').click();await p.locator('#drawingHome').waitFor({state:'hidden'});assert.equal(await p.evaluate(()=>ASMachApp.state.fileName),'Son çalışma.png');console.log('PASS PDF-area home, persistent saved/recent lists, reopen, no separate start screen');}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
